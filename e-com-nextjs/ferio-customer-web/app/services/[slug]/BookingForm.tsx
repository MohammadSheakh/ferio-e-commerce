'use client';import{FormEvent,useState}from'react';import type{ServiceOffering}from'@/lib/services';export default function BookingForm({service}:{service:ServiceOffering}){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/services/book',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({serviceId:service.id,customerName:f.get('name'),phone:f.get('phone'),email:f.get('email')||undefined,preferredAt:new Date(String(f.get('preferredAt'))).toISOString(),address:f.get('address')||undefined,customerNote:f.get('note')||undefined})});const p=await r.json();setMsg(r.ok?`Booking requested. Reference: ${p.data.reference}`:p.message||'Unable to book.');if(r.ok)e.currentTarget.reset()}const c='mt-1.5 w-full rounded-card border border-line px-4 py-3 text-[14px] outline-none focus:border-ink';return <form onSubmit={submit} className="mt-8 space-y-4 border-t border-line pt-7"><h2 className="text-[16px] font-medium text-ink">Request a booking</h2><div className="grid gap-4 sm:grid-cols-2"><label className="text-[12px] text-ink2">Name<input name="name" required className={c}/></label><label className="text-[12px] text-ink2">Phone<input name="phone" required className={c}/></label><label className="text-[12px] text-ink2">Email<input name="email" type="email" className={c}/></label><label className="text-[12px] text-ink2">Preferred time<input name="preferredAt" required type="datetime-local" className={c}/></label></div><label className="block text-[12px] text-ink2">Service address<input name="address" className={c}/></label><label className="block text-[12px] text-ink2">Note<textarea name="note" rows={3} className={c}/></label>{msg&&<p className="text-[13px] text-ink2">{msg}</p>}<button className="rounded-full bg-ink px-6 py-3 text-[13px] text-white">Request booking</button></form>}
