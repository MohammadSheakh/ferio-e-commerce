import { getPublicApi } from '@/lib/backend'; export type ServiceOffering={id:string;name:string;slug:string;description:string;price:number;durationMinutes:number;leadTimeHours:number;serviceAreaNote:string|null;requirements:string|null;imageUrl:string|null;category:{name:string}}; export const getServices=()=>getPublicApi<ServiceOffering[]>('/services',{cache:'no-store'}); export const getService=(slug:string)=>getPublicApi<ServiceOffering>(`/services/${slug}`,{cache:'no-store'});
