# 5 September 2026 Brutal Honest Opinion Fix Track

This tracker records remediation work for
`5-sep-2026-brutal-honest-opinion.md`. The opinion document is an audit record
and must not be modified to make the project look better.

## 2026-09-08 tenant service database boundary

- Removed the final feature-service-specific environment check from `AuditService`; it now uses the shared `resolveTenantDatabase` policy like the other tenant services. Extended `architecture:check` to reject feature services that inject `PrismaService` without the shared resolver or issue direct singleton queries. The direct singleton tenant request-path checklist item is now complete; nested-service client reuse remains open.

## 2026-09-08 CI release evidence alignment

- Added required dependency installation/audit gates for every backend and web lockfile, added tenant and control-plane migration deployment against disposable PostgreSQL, and added OpenAPI client drift checks to each web build job. This makes the CI evidence match the Release 1 checklist; rollback testing, full frontend E2E coverage, and production infrastructure drills remain open.

## 2026-09-08 tenant migration fleet evidence

- Added a CI integration case that creates ten disposable tenant databases, migrates them in parallel, verifies a shared schema head, and checks readiness for every database. Added migration-orchestrator coverage proving a failed middle tenant does not prevent later healthy tenants from completing. Full canary-to-fleet production drills and old/new schema compatibility tests remain open.

## 2026-09-08 provider entitlement enforcement

- Completed the previously open integration/provider entitlement control. Tenant-scoped payment-provider enablement now requires the `online_payments` entitlement, and courier activation requires `couriers_basic`, both through the shared `PlanGateService` evaluator. Legacy single-tenant mode remains unchanged because it has no tenant context. Added denial-code regression coverage; transactional messaging provider tenancy remains a separate open control.

## 2026-09-08 schema ownership freeze

- Completed the schema ownership freeze for new control-plane tables. `architecture:check` now verifies every model in `prisma/platform.prisma` is explicitly listed in the `CONTROL_PLANE` section of `data-classification.md`, so adding a global table requires a reviewed classification update in the same change.

## 2026-09-08 tenant identity binding reconciliation

- Reconciled the previously partial account-membership control. Admin sessions are bound by `TenantMembershipGuard`; customer accounts and rider actions resolve identity against the current tenant database and approved tenant-local records, with focused isolation tests. Full multi-client E2E coverage remains open and is not counted as complete here.

## 2026-09-08 automated organization provisioning reconciliation

- Reconciled the organization provisioning gate as complete for the implemented automated path. Platform Admin triggers the resumable provisioning state machine, which creates/registers the tenant database through the provisioner abstraction, applies migrations, seeds the baseline, checks readiness, and activates the domain without manual SQL. Managed production provider selection remains an infrastructure decision and is not represented as completed.

## 2026-09-08 tenant response cache boundary

- Added a backend cache boundary after successful tenant resolution: tenant responses are `private, no-store` and vary by `x-forwarded-host`, while existing `Vary` values are preserved. Added middleware regression coverage to prevent shared proxy/CDN responses from crossing storefront hosts without changing Redis behavior.

## 2026-09-08 tenant transaction entry boundary

- Added an architecture check for tenant feature transaction entry points. Feature services must resolve the local `db` client through `this.db()` before calling `db.$transaction`; direct singleton transaction calls are rejected by CI. Nested-service client reuse remains a separate open review item.

## 2026-09-08 canonical storefront redirect

- Added a production Customer Web middleware redirect for trusted-proxy HTTP safe requests, preserving the original host/path/query while returning a permanent HTTPS redirect. Unsafe API mutations are not redirected; same-origin enforcement remains authoritative. Certificate issuance and managed TLS automation remain infrastructure-owned.

## Status Legend

- `TODO`: identified, not yet implemented
- `IN PROGRESS`: currently being implemented
- `DONE`: implemented and verified
- `BLOCKED`: requires an explicit product, infrastructure, or migration decision

## Remediation Queue

| ID | Finding | Status | Evidence / next action |
| --- | --- | --- | --- |
| BO-01 | Backend typecheck excludes production runtime modules and `any` remains widespread. | IN PROGRESS | Added an entrypoint-based application typecheck and CI gate; active non-generated runtime/test boundaries are now typed, while generated Prisma declarations and isolated legacy trees remain separately scoped. |
| BO-02 | ESLint permits explicit and unsafe `any` usage. | IN PROGRESS | Strict rules are enabled and the remaining performance, socket-isolation, plan-lifecycle, and auth-tenant test harness boundaries are now typed. Generated declarations and intentional Jest matcher text are not production type escapes; broad legacy-tree lint remains open. |
| BO-03 | Docker compose is development-oriented and unsafe as a production deployment. | IN PROGRESS | Added a production overlay that requires credentials, enables tenancy, removes host exposure, enables Redis authentication, pins images, waits for backend health, and applies CPU/memory bounds; TLS/ingress and orchestration remain deployment-owned. |
| BO-04 | Legacy database/Mongoose fallback paths remain in active backend modules. | IN PROGRESS | Production configuration now fails fast unless tenancy is enabled; remaining legacy fallback inventory and removal/isolation are still open. |
| BO-05 | API contracts are generated per app but not centrally enforced. | IN PROGRESS | Added non-mutating `api:check` commands and CI checks for all three web clients plus a backend `openapi:check` gate against CI PostgreSQL/Redis services; a shared client remains open. |
| BO-06 | CI lacks lint, frontend/mobile tests, migration verification, image smoke tests, and strict dependency failure policy. | IN PROGRESS | Backend `lint` is now read-only; mobile typecheck, frozen-lockfile dependency audit, migration deployment, and production Compose render/exposure checks are covered, while lint, frontend tests, image smoke tests, and migration rollback tests remain open. |
| BO-07 | Scalability claims lack load, saturation, failure, and multi-instance evidence. | IN PROGRESS | The resolver/database baseline and socket-isolation harness are now explicitly typed and remain repeatable evidence; HTTP, WebSocket scale, queue throughput, saturation, failure-injection, and multi-instance evidence remain open. |
| BO-08 | Frontend has repeated fetch/parsing patterns, manual types, `any`, and no coherent test strategy. | TODO | Establish shared typed request/error utilities and test critical auth, tenancy, checkout, and admin flows. |
| BO-09 | Agent and skill rules are better documented than enforced by tooling. | IN PROGRESS | Added an executable backend architecture-boundary check to CI; remaining tenancy, dependency, contract, and folder-convention rules still need enforcement. |

## Change Log

### 2026-09-08

- BO-04/BO-06: reconciled downgraded-tenant historical access. Plan changes update only the platform subscription reference, preserve tenant commerce records, and leave tenant-routed report/read paths available; the subscription regression test proves no destructive data operation occurs. Legal retention, billing-provider proration, and plan-specific historical entitlement policy remain separate operational work.
- BO-04/BO-06/BO-07: reconciled provider-secret encryption with the implemented payment/courier boundaries. AES-256-GCM envelopes, required master-key validation, secret-free readiness/audit projections, and focused round-trip tests now provide repository evidence; external KMS/Secret Manager migration remains open.
- BO-01/BO-04/BO-06: made platform plan edits historically explainable. Plans now carry an additive revision counter that increments atomically on operator updates, and create/update audit snapshots include the revision and normalized entitlement set; seeded plans remain at revision 1. Added a platform migration and checksum; full billing-provider version history remains outside this slice.
- BO-04/BO-06/BO-07: made tenant provider credential rotation explicit. Payment and courier configuration replacement now records `credentialsRotatedAt` in tenant-local Prisma records, atomically replaces the encrypted envelope, and returns/audits only bounded rotation metadata. Added the migration checksum and regenerated the Prisma client; application typecheck and migration integrity remain required. External KMS/Secret Manager rotation, provider scheduling, and live provider verification remain open.
- BO-03/BO-06/BO-07: reconciled the backup-before-high-risk-migration control with the migration rollback/forward-fix runbook. The runbook now serves as the repository evidence for verified backup gates and stale/missing-evidence abort behavior; provider-backed scheduling, restore execution, and live drills remain open.

- BO-01/BO-04/BO-09: completed platform-vs-tenant feature-flag separation. Added a typed `PlatformFeatureFlagsService` backed exclusively by the control-plane Prisma client, guarded list/upsert routes under dedicated `feature_flag:read` and `feature_flag:write` permissions, strict key/note validation, and append-only platform audit records. Added focused creation, update, and invalid-key tests; application typecheck, architecture validation, migration validation/integrity, and focused platform tests pass.
- BO-04/BO-05/BO-08: implemented tenant-local storefront branding. Added a forward-only `CommerceSettings` migration for HTTPS-only logo/social URLs, address, and an allowlisted theme preset; extended the guarded tenant admin settings API and public storefront contract; rendered the logo in the header and social/contact identity in the footer. Backend settings/catalog tests, application typecheck, customer-web TypeScript check, architecture validation, and migration integrity pass. Arbitrary theme tokens, provider credentials, and full tenant-admin UI coverage remain open.
- BO-04/BO-07: closed the tenant-aware customer authentication contract. Customer session cookies remain host-only, storefront BFF requests forward the resolved host, tenant-issued tokens include the resolved organization, and refresh rejects a token issued for another organization. Added regression coverage for token binding and cross-tenant refresh rejection; the auth suite passes 6/6 and application typecheck remains green.
- BO-05/BO-08: completed the approved tenant theme boundary. The tenant API accepts only three named presets and the storefront maps them to bundled CSS variables; tenant input cannot provide arbitrary CSS, style tokens, scripts, or selectors. Customer-web TypeScript validation passes; visual browser coverage remains part of the frontend test gap.

### 2026-09-05

- Created this tracker; the audit document remains unchanged.
- Started BO-06 by separating read-only lint validation from auto-formatting.
- Read-only backend lint currently fails with 1,566 errors and 21,563 warnings, including project-service parsing failures for integration tests. A CI gate was not kept in a known-red state.
- Added `ferio-nest-prisma/tsconfig.application.json` as a full runtime typecheck boundary; the existing default build/typecheck configuration remains unchanged until newly exposed modules are repaired.
- Refined the application typecheck to follow `src/main.ts`, `src/app.module.ts`, and `src/platform/platform.module.ts`; it passes without compiling orphaned legacy duplicate trees.
- Added `pnpm typecheck:application` and its CI gate. This verifies the active dependency graph but does not close the explicit-`any` work.
- Replaced global request `any` types with `UserPayload` and typed upload metadata; tightened shared guards, user decorator, logging interceptor, exception filter, and upload interceptor. The application typecheck remains green.
- Replaced Firebase credential and notification payload `any` boundaries with `admin.ServiceAccount`, required credential checks, `unknown` error handling, and `Record<string, string>` message data. The application typecheck remains green.
- Added `docker-compose.production.yml` as a hardened overlay; local compose remains unchanged for development. The overlay requires production credentials, sets `NODE_ENV=production` and `TENANCY_ENABLED=true`, removes host ports for infrastructure/apps, and configures Redis authentication.
- Rendered the production overlay with temporary validation values and verified Compose produces no `published` host ports, production mode is enabled, tenancy is enabled, and Redis uses `--requirepass`. TLS, ingress, image pinning, resource limits, and orchestration remain open.
- Prevented production Compose from inheriting local platform superadmin bootstrap credentials; first operator provisioning must be deliberate and secret-managed.
- Added non-mutating `api:check` scripts to customer web, tenant admin, and platform admin; CI now compares generated OpenAPI client schemas with committed artifacts without modifying the worktree.
- Regenerated all three committed client schemas from `ferio-nest-prisma/openapi.json`; the drift included storage routes, two-factor DTO fields, and email format metadata. Customer web and tenant-admin `api:check` pass locally; platform-admin dependency installation is still missing locally but its generated artifact matches the verified output hash.
- Added mobile to the CI dependency-audit matrix and added a dedicated Expo TypeScript-check job using the existing `pnpm typecheck` script.
- Added fail-fast environment validation: `NODE_ENV=production` now requires `TENANCY_ENABLED=true`, preventing accidental startup in legacy single-tenant mode. Legacy fallback code still requires module-by-module removal or isolation.
- Extended production validation to require the platform database URL, platform JWT secret, platform credential-encryption key, and Redis password. Development/test defaults remain available only outside production.
- Added `TenantDbService.getOrLegacy()` as the single fail-closed policy for legacy database access. Migrated catalog, customer-account, and refunds services to use it, with regression coverage for legacy mode, missing tenant context, and resolved tenant context. Remaining feature services still need migration.
- Continued BO-04 migration across checkout, order, and cart services. These commerce paths now use the centralized fail-closed tenant database policy instead of silently falling back when tenancy is enabled.
- Migrated authentication and user-management database selection to the same centralized policy, removing duplicate environment-based fallback logic from the identity boundary.
- Migrated the remaining 22 feature-service inline fallbacks to `TenantDbService.getOrLegacy()`. Updated the storefront analytics unit test double to match the production boundary; the affected feature suite must remain green before this batch is committed.
- Removed equivalent fallback variants from chatting, staff access, OAuth, two-factor, courier routing, settlement imports, operations health, and transactional messaging. These paths now share the same fail-closed tenancy policy.
- Removed the remaining identity and WebSocket service fallback variants from user profiles, devices, socket authentication, and socket rooms. Queue-specific tenancy guards remain separate because they resolve organization identity from job payloads rather than request context.
- Audited Mongoose usage for BO-04: the remaining Mongoose models and Mongo database module are legacy/orphaned compatibility code, excluded from the active application typecheck and not imported by `AppModule`; the only `MongooseModule.forRootAsync` registration is commented out. They remain cleanup debt, but are not an active production database path.
- Started BO-02 explicit-`any` reduction in delivery personnel: replaced update payloads, filters, transaction clients, and shipment status maps with domain/Prisma types. Application typecheck passes; the module currently has no unit tests, which remains a testing gap.
- Removed all explicit `any` usage from cart saved-cart serialization and reorder/import paths by introducing typed Prisma-derived records, result contracts, and `unknown` error handling. Cart tests pass (7 tests) and the application typecheck remains green.
- Started the WebSocket BO-02 pass by typing socket system stats and connection metadata, and fixed the offline push projection so `getUserProfile()` selects the `fcmToken` that `emitToUser()` actually consumes.
- Corrected the WebSocket offline-push projection to read the latest active push-enabled device from `User.devices`, then typed the resulting token access. Socket tests pass (24 tests); broader gateway event payload typing remains open.
- Typed generic WebSocket server-emission payloads as `unknown`, added narrow record/string projections for push notifications, and removed unsafe payload access from the notification emission path. Socket tests and application typecheck pass; client-message handler DTO validation remains open.
- Hardened the `new-message-received` and `send-message` gateway inputs: payloads now enter as `unknown`, conversation/message IDs and timestamps are normalized explicitly, and persistence errors use `unknown` handling. Socket tests pass (24 tests); remaining gateway event payloads still need DTO extraction.
- Completed the next socket type-safety slice: sender persistence uses an explicit ID contract, room auto-join accepts the Socket.IO `join` capability, activity feeds return `unknown[]`, and the gateway/auth/room production files now contain zero explicit `any` occurrences. Socket tests and application typecheck pass.
- Replaced the OTP service's Redis `any` cast with the typed atomic `GETDEL` command and updated its test double. OTP tests pass (1 test) and the application typecheck remains green. Legacy Passport strategy `any` usage is isolated in an excluded compatibility tree for separate migration.
- BO-03: production Compose now requires explicit immutable image references for Postgres, Redis, MinIO, and the MinIO client instead of inheriting mutable development tags. Development Compose remains unchanged; production deployment must provide the image-reference variables.
- BO-03: fixed production readiness paths exposed by the hardened overlay: Redis healthchecks now authenticate against the required password, and the production MinIO initializer receives credentials through its environment and passes them correctly to `mc alias set`.
- BO-03: production customer/admin web builds now require public API, socket, and customer-site URLs as build arguments; runtime environment values are also set. This prevents Next.js `NEXT_PUBLIC_*` bundles from shipping development `localhost` endpoints behind a reverse proxy.
- BO-03: production web services now wait for the backend healthcheck instead of only waiting for the backend container process to start, preventing early frontend/API race failures during migrations and startup warm-up.
- BO-05: backend OpenAPI export now accepts `OPENAPI_OUTPUT`, and `pnpm openapi:check` compares a temporary export with the committed `openapi.json` without mutating the worktree. CI enforcement is intentionally deferred because the current full Nest bootstrap eagerly connects Redis, tenant Prisma, control-plane Prisma, and queue infrastructure; the next fix is an infrastructure-free documentation bootstrap, not a flaky network-dependent CI step.
- BO-05: enabled the backend OpenAPI drift gate in CI using the job's disposable PostgreSQL and Redis services, with isolated CI database/Redis credentials and no committed artifact mutation. Local contract execution still requires those services; application typecheck and workflow YAML validation pass.
- BO-03: added explicit CPU and memory limits/reservations to the production overlay for PostgreSQL, Redis, MinIO, backend, and all web services; MinIO initialization is bounded as well. Docker Compose rendering could not be executed in this environment because the Snap Docker installation refused to start, so the overlay must be rendered on the Docker-enabled host/CI runner.
- BO-01/BO-02: removed the remaining explicit `any` boundaries from the shared base entity contract and file-upload validation pipe, replacing them with string identifiers, `unknown` options, and an `unknown` transform input. The active application dependency-graph typecheck passes; legacy queue/Mongoose/Passport compatibility trees remain separately classified debt.
- BO-06: CI now applies both tenant Prisma migrations and platform Prisma migrations to the disposable PostgreSQL service before contract generation and tests. This verifies migration history is deployable from an empty database; rollback testing and production image smoke tests remain open.
- BO-06: dependency-audit is now a blocking critical-vulnerability gate. Each matrix app installs from its own frozen pnpm lockfile before `pnpm audit --prod --audit-level critical`; the previous advisory `continue-on-error` and ad hoc global pnpm install were removed.
- BO-06/BO-03: added a Docker-enabled CI job that renders the production Compose overlay with disposable values and fails if any host `published` port is present. This validates required variables, immutable image-reference syntax, and exposure hardening without pulling or starting production images.
- BO-03: corrected the base Compose documentation so the development stack is no longer described as production-capable; production use must go through the hardened overlay and an ingress/orchestrator.
- BO-07: added an explicit `test:performance` command and CI step for the existing tenant resolver, bounded connection-manager, LRU, and tenant bootstrap baselines. This is measurable baseline evidence, not a million-user capacity claim; broader saturation and multi-instance testing remain open.
- BO-07: fixed the performance baseline's missing-database guard so local runs skip only PostgreSQL-dependent cases instead of failing with an invalid URL. The local resolver baseline passes (2,000 cached resolutions in 14ms in this run); CI still runs the real PostgreSQL cases through `TEST_DATABASE_URL`.
- BO-09: added `pnpm architecture:check`, which strips comments before asserting that the active `AppModule` and legacy Mongo module do not register a Mongoose root connection, and verifies the hardened production Compose overlay exists. CI now runs this boundary check before migrations and application tests.
- BO-03/BO-06: strengthened the production Compose CI assertions to require rendered `NODE_ENV=production` and `TENANCY_ENABLED=true` in addition to rejecting host-published ports. This prevents a syntactically valid but semantically development-mode overlay from passing.
- BO-03: fixed the production Redis healthcheck credential boundary. Redis now receives `REDISCLI_AUTH` inside the container and the healthcheck uses authenticated `redis-cli ping`, instead of referencing a host-side variable that was never present in the container environment.
- BO-03/BO-06: hardened all three Next.js runtime images to run as the built-in non-root `node` user, matching the backend's existing non-root runtime. The production Compose CI job now rejects web Dockerfiles that omit this runtime boundary.
- BO-03/BO-06: added `init: true` and `no-new-privileges:true` to the production backend and web services, with CI assertions for every application service. This reduces zombie-process and privilege-escalation risk without requiring broad capability drops that could break database/storage images.
- BO-01/BO-02: typed the live Chatting message boundaries with Prisma-generated payloads and `Prisma.MessageWhereInput`, removed four production `any` usages, and aligned the send-message projection with its emitted attachment payload. The broader Chatting/Mongoose compatibility area remains excluded from the application typecheck and requires a separate migration slice.
- BO-01/BO-02: removed four additional live `any` boundaries from attachment uploads, Cloudinary responses, and queue processor error paths; the notification processor also no longer carries an unused untyped queue dependency. The upload endpoint still exposes preserved Multer file metadata under the historical `attachmentIds` response key, so that API contract needs a deliberate follow-up rather than a silent behavior change.
- BO-01/BO-02: hardened the email worker with an `unknown` job payload and required-string validation, and added runtime validation before JWT claims are accepted as `UserPayload`. The excluded Passport compatibility tree still contains OAuth `any` payloads, while the Local strategy references a missing `AuthService.validateUserForLocalAuth` method; these must be migrated or removed as a deliberate authentication decision, not papered over with casts.
- BO-04/BO-07: hardened shipping webhook, shipping polling, and payment recovery workers by removing unchecked tenant-data casts and fanout non-null assertions. Payment recovery now rejects tenant-enabled jobs without an organization ID, and all tenant fanout dependencies fail explicitly when unavailable; application typecheck, architecture validation, and payment callback tests pass.
- BO-01/BO-02: hardened the shared legacy database exception filter by replacing its exception/request/response `any` casts with unknown narrowing, typed authenticated-request access, safe duplicate-key metadata extraction, and an explicit error response contract. Both application and default backend TypeScript checks pass; the filter remains legacy compatibility code and is not exported by the active common barrel.
- BO-01/BO-02/BO-09: synchronized the duplicate `libs/queue` processor copies with the typed active queue workers, removed stale queue `any` contracts, and introduced explicit package-local `AuthenticatedRequest`/`UploadRequest` types for common guards and interceptors. The Nest queue build, application typecheck, default backend typecheck, and diff validation pass; root ambient declarations remain only as compatibility support.
- BO-02/BO-04/BO-07: removed unchecked organization casts and fanout non-null assertions from reconciliation and transactional-message workers, and removed repeated socket tenant casts in message relay. Reconciliation retry IDs and transactional message IDs are now narrowed through queue contracts; application typecheck, architecture validation, and 17 focused transactional/socket tests pass.
- BO-04/BO-07: added direct queue-worker regression coverage for payment recovery, transactional messaging, and reconciliation tenant boundaries. The suite verifies missing organization context is rejected, missing fanout fails safely, and stamped jobs execute through the correct tenant fanout; 5/5 tests pass.
- BO-04/BO-09: removed the `registry as never` escape hatch from `TenantFanoutService` and modeled fanout registry data as tenant database connection material plus the required control-plane organization ID. Fanout pagination/concurrency and worker boundary suites pass 10/10, with application typecheck and architecture validation green.
- BO-04/BO-09: removed tenant registry `as never` escapes from retention and usage reconciliation, using the bounded manager method contracts directly. Retention and usage tests pass 12/12 with application typecheck green; retention still has one isolated dynamic-delegate cast because rules select among multiple Prisma models by key, which remains a focused follow-up.
- BO-04/BO-09: also removed the tenancy module's `platform.client as never` injection escape; the platform membership client now satisfies the service contract directly under application typecheck.
- BO-01/BO-02/BO-09: introduced `toPlatformJsonInput()` for control-plane JSON fields and replaced audit/support-access `as never` writes with normalized Prisma JSON inputs. Dates, nested records, arrays, nullish values, and unsupported primitives are covered by tests; platform service plus normalizer tests pass 21/21.
- BO-01/BO-02/BO-09: extended the platform JSON contract to billing gateway raw evidence, provisioning step details, and tenant migration results/audit metadata. Platform billing tests pass 7/7 and migration/organization tests pass 13/13; application typecheck remains green.
- BO-01/BO-02: removed shipping polling status `as never` casts by separating Prisma enum query literals from the runtime terminal-status set. All shipping suites pass 13/13 and application typecheck remains green.
- BO-01/BO-02: replaced authentication JWT expiry `as never` casts with a validated `jwtExpirySeconds()` contract. Approved `s/m/h/d` configuration is converted to numeric seconds for module registration and token issuance; invalid values use the documented development fallback and invalid fallbacks fail explicitly. Application typecheck, formatting, and focused authentication/config tests pass 13/13.
- BO-01/BO-02: removed the unnecessary `as unknown as ReportOrder[]` cast from the bounded reports aggregation; Prisma's generated `findMany` result already satisfies the selected `ReportOrder` contract. Reports tests pass 9/9, architecture validation and application typecheck remain green.
- BO-01/BO-02: narrowed the unavoidable R2 presigner compatibility adapter from `unknown` client/command parameters to `S3Client` and the concrete PUT/GET command types. The adapter remains a temporary dependency boundary because duplicate `@smithy/types` versions make the official SDK generic type incompatible; deduplication is the follow-up.
- BO-01/BO-02: removed settlement import and row snapshot `as unknown as Prisma.InputJsonValue` casts by adding a shared tenant Prisma JSON normalizer. Settlement DTO instances are converted into explicit JSON-safe objects before persistence, with nullish omission, date serialization, array handling, and unsupported primitive normalization covered by tests. Typecheck, settlement/parser tests pass 15/15, and architecture validation remains green.
- BO-01/BO-02: applied the tenant JSON normalizer to storefront analytics filters, refund results, payment gateway evidence, shipping requests/responses, and polling evidence. This removes provider-payload `as Prisma.InputJsonValue` casts while preserving optional-field omission and required JSON-column defaults. Focused payments/refunds/shipping/analytics tests pass 29/29, with application typecheck and architecture validation green.
- BO-01/BO-04: removed additional production non-null assertions from tenant membership invalidation, reconciliation duration metrics, settlement COD records, and customer metrics aggregation. Optional Redis is captured before the async invalidation callback; settlement records reuse the validated COD relation; incomplete aggregate rows are ignored safely. Application typecheck and focused reconciliation/settlement/customer/tenant-membership tests pass (9 suites, 37 tests).
- BO-01/BO-04: removed the retention sweep's `TenantClient` cast and synthetic client interface. Retention now uses the generated tenant `PrismaClient` directly, with an explicit four-model `RetentionModel` union governing dynamic delegate selection. Retention and tenant database manager tests pass 13/13, with application typecheck and architecture validation green.
- BO-01/BO-02: removed four orphaned Passport strategies (local, JWT, Google, and Apple) that were not registered or referenced by the active application and included a broken `validateUserForLocalAuth` call plus explicit `any` contracts. Removed the unused `PassportModule` registration and dependency; active authentication continues through the custom `AuthGuard`, JWT service, and verified Google ID-token flow. Focused auth/OAuth tests pass 10/10 and application typecheck remains green.
- BO-01/BO-02/BO-03: removed the R2 presigner `as unknown as` adapter by aligning `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner` to `3.1118.0`. The dependency tree now resolves one `@smithy/types` version, allowing direct typed presigner calls. Application typecheck, formatting, architecture validation, and diff checks pass; the active production source has no remaining explicit type escape, excluding comments and generated/legacy code.
- BO-06/BO-09: added a read-only `lint:application` gate for the active Nest entrypoints (`main.ts`, `app.module.ts`, and `platform.module.ts`) and wired it into backend CI beside architecture validation. The gate, active application typecheck, architecture check, and diff validation pass; broad linting of excluded legacy trees remains intentionally open.
- BO-01/BO-02/BO-06: enabled `@typescript-eslint/no-explicit-any` as an error in the backend ESLint configuration and removed the last stale `Model<any>` reference from commented queue code. Active lint, default strict typecheck, application typecheck, full backend build, architecture validation, and diff checks pass; broad legacy-tree lint remains a separate migration boundary.
- BO-01/BO-02/BO-06: enabled `@typescript-eslint/no-unsafe-call` and `@typescript-eslint/no-unsafe-argument` as errors. The active application entrypoint lint passes with the stricter policy, alongside default strict typecheck, application typecheck, architecture validation, and diff checks; excluded legacy-tree violations remain open.
- BO-04/BO-09: removed the orphaned Mongo/Mongoose runtime surface: legacy Mongo root module, attachment CRUD module/service/controller, message-read-status service, and three unused legacy queue processors. Pruned `@nestjs/mongoose` and `mongoose` dependencies, preserved the Cloudinary adapter still used by warranty, and updated the architecture guard to accept complete legacy-module removal. Strict typecheck, application typecheck, backend build, lint, architecture, and diff checks pass.
- BO-04/BO-09: removed repeated `this.prisma as PrismaClient` upcasts from tenant-facing catalog, cart, checkout, identity, order, reports, refunds, returns, shipping, settings, and service modules. The centralized `TenantDbService.getOrLegacy()` policy remains the only fallback decision; tenant regression coverage, strict/application typechecks, backend build, lint, architecture, and diff checks pass.
- BO-01/BO-04/BO-09: removed the remaining redundant `this.prisma as PrismaClient` upcasts from authentication, chatting, commerce payments, notifications, operations health, purchase activity, reconciliation, settlements, sockets, staff access, storefront analytics, transactional messaging, user management, wallet, warranty, and store-location services. The centralized `TenantDbService.getOrLegacy()` policy is now consistently expressed without client-type assertions across the feature tree; application lint/typecheck, strict backend typecheck, build, architecture validation, focused tenant regression tests, and diff checks pass.
- BO-01/BO-04/BO-09: removed the inactive duplicate queue architecture and remaining Mongoose-era queue processors. The active `libs/queue` registry now contains only live queue names, feature-owned processors remain the runtime handlers, the unused `src/core/queue` implementation is gone, and the common ObjectId pipe no longer imports Mongoose. Common/queue builds, application and strict typechecks, full backend build, lint, architecture validation, focused queue/security tests, and diff checks pass.
- BO-01/BO-09: removed historical TypeScript exclusions for active Chatting, Socket Gateway, and attachment code plus deleted migration-era paths. The default backend typecheck and production build configuration now inspect the active feature tree instead of silently excluding those modules; strict typecheck, full build, application lint, architecture validation, focused Chatting/Socket tests, and diff checks pass.
- BO-01/BO-02: removed the remaining production JSON and transaction-client type assertions from settings, audit, transactional messaging, platform JSON normalization, and delivery personnel. These paths now use the shared runtime JSON normalizer or inferred Prisma transaction contracts; no `as unknown as`, `as Prisma.InputJsonValue`, `as never`, or transaction-client assertions remain in non-test backend source. Strict/application typechecks, full build, lint, architecture validation, focused JSON/audit/settings/messaging/tenant tests, and diff checks pass.
- BO-04/BO-09: hardened tenant cache and membership infrastructure against untrusted runtime data. Redis invalidation payloads and positive tenant-resolution cache entries are now parsed as `unknown` and structurally validated; cached hosts must match the requested normalized hostname, subscription states are validated before context creation, and membership uses typed platform/Redis contracts. Resolver, pub/sub, membership, collision, and tenant database tests plus strict/application typechecks, build, architecture validation, and diff checks pass.
- BO-01/BO-02/BO-09: hardened Settings pagination query boundaries by allowlisting the `SettingsType` filter and supported sort columns instead of forwarding arbitrary request keys into Prisma. Unknown filters are ignored and unknown sort fields fall back to a safe column; focused settings boundary tests cover both paths, with strict/application typechecks, full build, lint, architecture validation, and diff checks green.
- BO-01/BO-02/BO-04: hardened runtime configuration and Redis feed parsing in checkout, transactional messaging, and Socket Gateway. Coupon JSON is structurally validated before use, messaging channel lists use generated enum types without assertions, and malformed activity entries are skipped safely instead of crashing the feed. Focused coupon/messaging/socket tests, strict typecheck, full build, lint, architecture validation, and diff checks pass.
- BO-01/BO-04/BO-07: replaced generic Redis cache deserialization with an explicit runtime parser contract. User, profile, and settings caches now validate their shapes before returning cached data; malformed or stale values become source-of-truth fetches and repair the cache. Redis, identity, settings, strict/application typechecks, full build, lint, architecture validation, and diff checks pass.
- BO-06/BO-09: made the repaired application lint gate deterministic by invoking the installed ESLint package entrypoint directly instead of relying on the broken local `.bin/eslint` shim, and added the read-only `lint:application` step to backend CI. The active entrypoints pass; broad source lint remains honestly tracked at 1,638 errors and 21,558 warnings and is not promoted to a required gate until its legacy baseline is repaired.
- BO-01/BO-02/BO-06: repaired the shared `libs/common/src` runtime boundary under the strict ESLint rules. Common guards now use typed request/JWT payloads, upload pipes and interceptors avoid unsafe request access, response transformation preserves its generic envelope without `any`, and logging sanitizes unknown values without implicit object stringification. The complete common source package now has zero ESLint errors; strict/application typechecks, application lint, architecture validation, and diff checks pass. Broad legacy-tree lint remains separately tracked and is not claimed complete.
- BO-02/BO-06: repaired the queue email worker boundary. The queue library no longer excludes its live processor from its own TypeScript project; the worker now depends on a narrow delivery contract token, is registered by the authentication feature while queue infrastructure remains dependency-direction safe, and is backed by the existing `EmailService`. This closes a real runtime gap where email jobs could be queued without a registered consumer. Queue/notification/auth lint has zero strict errors; queue compilation, application and strict typechecks, full backend build, application lint, architecture validation, queue smoke test, and diff checks pass.
- BO-01/BO-02/BO-06: removed the misleading `async` implementation from `RedisService.getClient()` while preserving its `Promise<Redis | null>` contract for existing callers and test doubles. Database, Redis, and notification shared infrastructure now report zero strict ESLint errors; application/strict typechecks, full build, application lint, architecture validation, queue smoke test, and diff checks pass. A broad platform/tenancy lint scan was not counted because the process produced no report in this environment.
- BO-01/BO-02/BO-06: completed the authentication and user-management strict type-safety slice. OTP Redis records are now parsed through a runtime `OtpRecord` guard; simulated email delivery methods return explicit promises and consume their inputs; dead imports and an unnecessary auth-provider assertion were removed; and identity tests now inspect typed mock-call contracts instead of propagating Jest `any`. Authentication and user-management source/tests report zero strict ESLint errors; focused identity tests, application/strict typechecks, full build, application lint, architecture validation, queue smoke test, and diff checks pass.
- BO-01/BO-02/BO-04: hardened the commerce payment trust boundary. AamarPay and SSLCommerz gateway responses now use scalar-only text normalization instead of implicit object stringification, preventing malformed provider payloads from becoming misleading transaction IDs, URLs, statuses, or currencies. Checkout and the complete commerce-payment tree now report zero strict ESLint errors; payment service/recovery tests use typed transaction, query, gateway, and audit call contracts instead of unsafe Jest matcher data. Focused commerce/checkout tests, application/strict typechecks, full build, application lint, architecture validation, and diff checks pass.
- BO-01/BO-02/BO-04: hardened shipping courier trust boundaries with shared scalar-only `shippingText()` normalization across CarryBee, eCourier, Paperfly, Pathao, RedX, and Steadfast adapters. Webhook identifiers, statuses, tracking values, and timestamps no longer implicitly stringify arbitrary objects; Pathao configuration reads are explicitly typed. Cart and order production cleanup also removed dead imports/assertions and replaced an over-broad saved-cart Prisma payload alias with an explicit serialized contract. Shipping production sources plus cart/order production services report zero strict ESLint errors; focused operational tests, application/strict typechecks, full build, application lint, architecture validation, and diff checks pass. Fifteen strict errors remain in cart/order/shipping test doubles and matcher-heavy assertions and are tracked separately.
- BO-01/BO-02/BO-04/BO-06: closed the remaining shipping/cart/order strict test-contract debt. Queue tests now use `CourierCallbackJobData`, inventory tests inspect typed movement and reservation calls, cart eligibility tests use an explicit Prisma query contract, and transaction callbacks no longer propagate Jest `any`. The complete shipping, cart, and order feature trees now report zero strict ESLint errors; focused operational tests, application/strict typechecks, full build, application lint, architecture validation, and diff checks pass. The previous 15-error test debt is resolved.
- BO-01/BO-02/BO-04/BO-06: hardened finance operations transaction boundaries by typing refunds, returns, reconciliation, and settlement test transaction callbacks, removing the settlement import `let`, and constraining reconciliation queue tests to `ReconciliationJobData`. Finance production paths report zero strict ESLint errors; focused refund/return/reconciliation/settlement tests, application/strict typechecks, full build, application lint, architecture validation, and diff checks pass. Nineteen matcher-heavy test assertions still use inferred Jest `any` and remain the next isolated test-contract slice.
- BO-01/BO-02/BO-06: closed the refunds, returns, and settlements strict test-contract slice. Financial create/update/movement assertions now inspect typed mock-call payloads directly, and settlement parser assertions use typed result fields instead of matcher-generated `any`. Refunds, returns, and settlements now report zero strict ESLint errors; focused finance tests, application/strict typechecks, full build, application lint, architecture validation, and diff checks pass. Reconciliation retains 10 matcher-heavy test assertions for the next isolated slice.
- BO-01/BO-02/BO-06: closed the reconciliation strict test-contract slice. Reconciliation finding, run-update, retry, and resolution assertions now inspect typed mock-call payloads instead of nested matcher-generated `any`; the transaction test double also models both Prisma callback and array forms. Reconciliation, refunds, returns, and settlements now pass 39/39 focused tests and report zero strict ESLint errors. Application/strict typechecks, full build, application lint, architecture validation, and diff checks pass. The earlier note that reconciliation retained 10 assertions is historical and superseded by this entry.
- BO-01/BO-02/BO-06/BO-09: tightened the platform control-plane test boundary for entitlements, organizations, subscriptions, and usage metering. Narrow local platform-client doubles replace explicit `any`, duplicate-organization errors use `unknown`-safe construction, transaction mocks model their callback contract, and matcher-heavy assertions inspect typed call payloads. The four specs report zero strict ESLint errors and pass 36/36 tests; generated Prisma declarations remain intentionally excluded from application lint because they are generated vendor output. The remaining platform and tenancy test inventory is the next type-safety slice.
- BO-01/BO-02/BO-06/BO-09: typed the tenant migration orchestrator state-machine spec. Platform run/result records, migration callbacks, status updates, and bootstrap call inspection now use explicit contracts instead of `any` and untyped mock parameters. The migration orchestrator spec reports zero strict ESLint errors and passes 4/4 tests; the complete platform services suite passes 55/55. Remaining tenancy specs and the larger migration/legacy cleanup remain open.
- BO-01/BO-02/BO-04/BO-06/BO-07: typed the retention-sweep tenancy harness. Dynamic retention delegates, delete-batch arguments, registry lookups, and fan-out callbacks now use explicit contracts instead of `any`; the retention spec reports zero strict ESLint errors and passes as part of the full tenancy suite (15 suites, 105 tests). Also corrected the malformed-positive-cache fixture in the tenant resolver test so the negative cache key returns empty data; the resolver now verifies control-plane fallback after rejecting cross-host cached data. The broader tenant-resolver and tenancy-spec type cleanup remains open.
- BO-01/BO-02/BO-04/BO-06/BO-09: completed the tenant-resolver spec contract slice. The service and platform doubles are explicitly typed, Redis cache doubles distinguish positive and negative keys, and forwarded-host assertions validate the concrete `TenantResolutionException.code` without unsafe Jest matchers. The resolver spec reports zero strict ESLint errors; the complete tenancy suite remains green at 15/15 suites and 105/105 tests. Remaining tenancy specs still require the same treatment.
- BO-01/BO-02/BO-04/BO-06/BO-09: completed the tenant membership and public tenancy controller spec slice. Membership roster lookups, request contexts, cache invalidation doubles, forwarded-host resolution, and stable controller error fixtures now use explicit contracts; cross-tenant replay and unauthenticated access assertions inspect typed calls. Both specs report zero strict ESLint errors and pass 12/12 focused tests; the complete tenancy suite remains green at 15/15 suites and 105/105 tests. Redis-collision and remaining tenancy support specs are still open.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: completed the Redis-collision isolation spec slice. Tenant resolver platform doubles, positive-cache client responses, SettingsService cache-key checks, OTP key checks, and tenant context contracts are now explicitly typed; the fixture now exercises the actual Redis `get` response shape. The spec reports zero strict ESLint errors and passes 7/7 tests; the complete tenancy suite remains green at 15/15 suites and 105/105 tests. Remaining tenancy support-spec cleanup is now limited to the unresolved legacy test inventory.
- BO-01/BO-02/BO-06: closed the wallet test-contract slice. The wallet transaction double now exposes its `findUniqueOrThrow` contract and types the Prisma transaction callback; ledger assertions inspect typed create payloads instead of nested matcher-generated `any`. The wallet spec reports zero strict ESLint errors, and wallet plus platform-services plus tenancy regression suites pass 24/24 suites and 164/164 tests. Remaining live explicit-`any` inventory is limited to generated Prisma declarations and the next isolated feature test boundaries.
- BO-03/BO-06/BO-07: hardened the tenant backup and restore runbook helpers. Backups now validate database identifiers, use private output permissions, write PostgreSQL custom archives directly, verify archive readability, and emit SHA-256 sidecars. Restores now require an explicit `restore_drill_<name>` target, verify the dump and optional checksum before creation, fail on inaccessible existing targets, and use `pg_restore --exit-on-error --no-privileges`; managed scheduling, immutable evidence storage, retention enforcement, and a provider-backed restore drill remain deployment-owned work.
- BO-01/BO-02/BO-06/BO-07: closed the remaining explicit runtime `any` boundaries in the performance baseline, live socket-isolation harness, plan-limit lifecycle harness, and auth/tenant-binding regression spec. Resolver results, Prisma mock inputs, socket wire events/waiters, plan subscription/usage doubles, and authenticated requests now use explicit contracts or `unknown` narrowing. Direct test-boundary TypeScript compilation, strict/application typechecks, application lint, backend build, architecture validation, formatting, and diff checks pass; Jest invocation in this environment emitted no test report, so this change claims type safety rather than new test execution evidence.
- BO-06: made every backend Jest script invoke the installed Jest entrypoint directly instead of relying on the local `.bin/jest` shim. This closes the reproducibility failure observed in the workspace, where `.bin/jest` was zero bytes and test commands exited without a report; unit, integration, performance, queue-smoke, coverage, watch, debug, and E2E scripts now share the deterministic entrypoint pattern already used by the application lint gate.
- BO-07/BO-09: fixed a real tenant-resolution cache-stampede race exposed after restoring Jest execution. Concurrent requests for the same uncached hostname now share one in-process control-plane resolution promise, with `finally` cleanup on success or failure; Redis remains the cross-instance cache. The performance baseline now verifies the intended two warm-up lookups instead of issuing one query per concurrent request.
- BO-06/BO-07: restored executable integration evidence by declaring `ws` and `@types/ws` as explicit backend development dependencies; the socket isolation harness no longer depends on an undeclared transitive package. With local TCP permission, the live two-tenant wire test passes 3/3 and the performance baseline passes 4/4 with 2,000 cached resolutions in 13 ms, two control-plane queries, one negative-cache lookup, one cold-burst coalescing check, and a 1 ms outage path. The PostgreSQL-backed plan-limit case remains skipped when `TEST_DATABASE_URL` is unavailable.
- BO-01/BO-02/BO-06: corrected two strictness defects exposed by the restored gates. The common response interceptor now constructs the declared response envelope instead of casting arbitrary records to `Response<T>`, and the two-factor regression spec narrows optional recovery hashes before indexing. Strict/application typechecks, application lint, backend build, architecture validation, and 29 focused unit tests pass.
- BO-03/BO-06: changed the Redis-backed rate-limit guard to fail closed in production when Redis is unavailable, the pipeline returns no result, or the counter result is invalid. Development/test remains fail-open, and production can explicitly opt into the availability trade-off with `RATE_LIMIT_FAIL_OPEN=true`; the default protects authentication, payment, and administrative routes during Redis degradation. The production outage regression passes with `503 SERVICE_UNAVAILABLE`, and the security-event suite passes 2/2.
- BO-03/BO-06: completed the rate-limit failure policy by rejecting Redis pipeline command-level errors as well as missing/invalid results. Production now fails closed for unavailable, partial, and malformed Redis evaluation; development/test behavior remains explicitly fail-open. The security-event suite now covers both outage and partial-pipeline failure paths.
- BO-01/BO-02/BO-06: hardened platform billing provider boundaries. SSLCommerz gateway fields now accept only scalar text/number/boolean values, preventing structured provider payloads from becoming `[object Object]` URLs, transaction IDs, statuses, or amounts; dead callback-string code and an unused entitlement import were removed, and the migration orchestrator uses a non-reassigned registry binding. Added regression coverage for structured gateway URLs; platform billing tests, strict typecheck, and targeted platform lint pass.
- BO-01/BO-04: removed the remaining active checkout actor non-null assertions. Wallet customer resolution and debit now use an explicit `requireActor()` contract after the optional actor boundary, preserving the existing authentication error while making the invariant executable. Order tests, strict/application typechecks, lint, build, architecture validation, and diff checks remain required before merge.
- BO-01/BO-02/BO-04: hardened reports and Cloudinary provider boundaries. The report accumulator now declares its method receiver instead of allowing implicit `this: any`; Cloudinary deletion results and callback errors are narrowed from `unknown` before use, preventing malformed provider output from reaching status/error logic. Reports/attachment tests pass 9/9; targeted strict lint, application typecheck/lint, architecture validation, and diff checks pass.
- BO-01/BO-02/BO-03: removed the untyped environment boundary from configuration validation. Raw environment values are now copied through an explicit string reader, normalized numeric settings use a declared config value type, and production/default validation behavior is preserved without unsafe assignments or calls. No dedicated config-validator spec exists yet; targeted strict lint, application typecheck/lint, architecture validation, and diff checks pass.
- BO-01/BO-02/BO-04: repaired the active Chatting conversation boundary. The participants endpoint now performs tenant-scoped membership authorization and returns real active participants instead of a placeholder; conversation search is applied consistently to results and totals; unused imports and an unsafe role enum comparison were removed. Chatting tests pass 3/3; targeted strict lint, application typecheck/lint, architecture validation, backend build, and diff checks pass.
- BO-01/BO-02/BO-04: closed the remaining strict production errors in the Chatting tree. JWT module registration no longer uses an unnecessary secret assertion, and the non-blocking message-read fallback no longer declares an unused catch value. The feature now reports zero strict ESLint errors; Chatting tests pass 3/3, application typecheck/lint, architecture validation, backend build, and diff checks pass.
- BO-01/BO-02/BO-09: hardened both tenant and platform JSON normalizers against unsupported primitive fallbacks. BigInt and symbol values now have explicit contracts, while functions and unsupported values are omitted instead of being generically stringified; regression tests pass 7/7, targeted strict lint, application typecheck/lint, architecture validation, backend build, and diff checks pass.
- BO-01/BO-02: closed a broad low-risk production lint slice across catalog, customer account, product content, reports, settings, storage, transactional messaging, and wallet. Removed redundant assertions/imports, made intentional empty recovery catches explicit, and replaced generic object stringification in CSV/message rendering with scalar/date-only contracts. Targeted strict lint is clean.
- BO-04/BO-06: repaired stale catalog tenant-routing test doubles to model the centralized `TenantDbService.getOrLegacy()` contract instead of the removed `tryGet()` API. Catalog and commerce-settings tests now exercise the same tenant/legacy selection boundary used in production; the affected module matrix passes 12 suites and 54 tests.
- BO-01/BO-02/BO-04/BO-09: tightened platform and tenancy request/dependency boundaries. Platform auth now uses a typed Express request and normalized authorization header; local PostgreSQL provisioning uses a static `pg` import; tenancy controllers/modules no longer use lazy `require()` calls, unused guards, or JWT secret assertions. Targeted strict lint passes; platform-auth, billing, and tenancy tests pass 16 suites and 113 tests; application typecheck/lint, architecture validation, backend build, and diff checks pass.
- BO-01/BO-02/BO-04/BO-06/BO-09: closed the remaining active production strict-lint slice after the Socket Gateway pass. Chatting callers now match synchronous socket emission contracts; delivery status and shipment enum assertions were removed; platform login uses a static exception import and payment-attempt reads no longer perform unused organization work; tenancy request headers, callback status, membership email, and resolver boundaries are explicitly typed; and unused provisioner/subscription parameters were made intentional. The active non-test `src` tree now reports zero strict ESLint errors. Affected Chatting/platform/tenancy/delivery matrix passes 25 suites and 168 tests; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: closed the tenant fan-out test contract boundary. Tenant registry pagination fixtures, bounded worker callbacks, tenant context reads, transient-manager operations, and failure isolation mocks now use explicit contracts instead of Jest-inferred `any`, dynamic `require()`, or unnecessary async callbacks. The fan-out spec reports zero strict ESLint errors and passes 5/5; the broader tenancy/tenant regression matrix passes 18 suites and 119 tests. Application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-03/BO-06: closed the platform billing provider-test contract boundary. Fetch response fixtures now use typed JSON helpers without async-without-await methods or repeated fetch assertions; payment-attempt and invoice mutation assertions inspect typed mock-call payloads instead of propagating Jest `any`. The platform billing spec reports zero strict ESLint errors and passes 8/8; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-09: closed the staff-access security-test contract boundary. Transaction callbacks, config fallbacks, invitation email/token fixtures, atomic token-consumption assertions, staff creation payloads, and session-revocation updates now use explicit types instead of Jest-inferred `any` and async callbacks without awaits. The staff-access spec reports zero strict ESLint errors and passes 5/5, preserving coverage for hashed invitations, concurrent-consumer rejection, self-deactivation prevention, and permission/session updates. Application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-09: closed the catalog service-test contract boundary. Transaction callbacks, category/product fixtures, inventory movement mocks, and Prisma create assertions now use explicit callback and payload types instead of Jest-inferred `any`; assertions preserve coverage for slug normalization, unsafe pricing, negative inventory, second-hand disclosures, category lifecycle, and publish prerequisites. The catalog spec reports zero strict ESLint errors and passes 10/10; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07: closed the transactional-message dispatcher test contract boundary. Queue update/create callbacks, provider dispatch results, retry payloads, and terminal-status assertions now use explicit types instead of async callbacks without awaits or Jest-inferred `any`. The dispatcher spec reports zero strict ESLint errors and passes 4/4; the full transactional-messaging matrix passes 3 suites and 12 tests, including definitive-failure fallback, duplicate-delivery protection, tenant database resolution, and strict fail-closed behavior. Application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07: completed the transactional-messaging service test contract boundary. Template update transactions, rendered-message upserts, and version assertions now use explicit callback and mock-call payload types instead of async callbacks without awaits or Jest-inferred `any`. The service spec reports zero strict ESLint errors and passes 3/3; the complete transactional-messaging matrix remains green at 3 suites and 12 tests, with application typecheck/lint, architecture validation, and backend build passing.
- BO-01/BO-02/BO-04/BO-06: closed the RTO service-test contract boundary. Prisma transaction callbacks and inventory/reservation/order update assertions now use explicit mock-call payload types instead of unsafe Jest matcher data; coverage still verifies stock settlement, reservation release, RTO cost calculation, cancellation state, and audit actor identity. The RTO spec reports zero strict ESLint errors and passes 3/3; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06: closed the commerce-settings service-test contract boundary. Transaction callbacks, normalized settings updates, and staged rollout flag assertions now use explicit mock-call payload types instead of unsafe Jest matcher data. The settings spec reports zero strict ESLint errors and passes 5/5, preserving coverage for normalization, audit coupling, timezone validation, and prepaid-provider safety. Application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-06: closed the store-locations test hygiene boundary. Removed unused payload/exception imports and unused injected service fixture variables without changing the public store listing, stock availability, conflict handling, creation, or audit behavior. The store-locations spec reports zero strict ESLint errors and passes 5/5; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07: closed the storefront-analytics test contract boundary. Search-event creation and zero-result group queries now inspect explicit mock-call payload types instead of unsafe Jest matcher assignments; measured funnel, revenue, order, and search-evidence coverage remains unchanged. The storefront-analytics spec reports zero strict ESLint errors and passes 5/5; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06: closed two remaining observability test boundaries. Customer-notification ownership/read-state assertions now use typed update-call payloads, and operations-health assertions now inspect typed health sections directly instead of unsafe nested Jest matchers. Customer notification and operations health tests pass 3/3 and 2/2 respectively; both specs report zero strict ESLint errors, with application typecheck/lint, architecture validation, and backend build passing.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: closed the tenant-worker boundary test hygiene slice. Synchronous rejection tests no longer use misleading async wrappers; coverage remains for missing organization rejection, stamped payment/transactional tenant execution, and reconciliation fan-out failure handling. The worker-boundaries spec reports zero strict ESLint errors and passes 5/5; application typecheck/lint, architecture validation, and backend build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: closed the tenant-membership pub/sub test boundary. The Redis duplicate/publish fake now uses lexical typed methods instead of aliasing `this`, unused callback parameters are explicit, and synchronous local-cache tests no longer use async wrappers. The pub/sub spec reports zero strict ESLint errors and passes 3/3; the tenancy regression matrix passes 15 suites and 105 tests, with application typecheck/lint, architecture validation, and backend build passing.
- BO-01/BO-02/BO-07: started the Socket Gateway strict-runtime pass. Removed unused guard wiring and assertions, typed Redis activity JSON parsing as `unknown`, removed an unused page-view/socket binding, and changed synchronous emission methods to honest synchronous contracts instead of `async` methods without awaits.
- BO-01/BO-02/BO-04/BO-07: completed the Socket Gateway runtime type boundary. Socket.IO `SocketData` is now explicit and shared by the gateway, authentication service, and JWT guard; verified JWT claims are normalized into `SocketUser`; handshake inputs and activity feed JSON are narrowed from `unknown`; room handlers reject missing authenticated IDs; and synchronous emission methods expose synchronous contracts. The complete Socket Gateway tree reports zero strict ESLint errors, socket tests pass 25/25, application typecheck/lint, architecture validation, and backend build pass. Remaining broad lint debt and scalability/load evidence remain open under their existing items.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: closed the tenant database manager test contract boundary. Circuit-breaker failure assertions now narrow caught `unknown` errors before reading messages, and the credential-encryption regression test uses a synchronous contract because it performs no asynchronous work. The manager spec reports zero strict ESLint errors and passes 9/9; application typecheck/lint, architecture validation, and the multi-target Nest build pass.
- BO-01/BO-02/BO-04/BO-06/BO-07/BO-09: closed the remaining repository-wide strict test boundaries. Application health, error-contract, rate-limit, reports, warranty, tenant-closure, callback-token, tenancy-observability, and tenant-admin guard specs now use explicit unknown narrowing and typed call inspection instead of Jest-inferred `any` or redundant assertions. The complete backend `src` tree reports zero strict ESLint errors; the affected regression batch passes 9 suites and 45 tests; application typecheck/lint, architecture validation, and the multi-target Nest build pass.
- BO-01/BO-02/BO-06/BO-09: made the backend strict-lint result reproducible and CI-enforced. Added `lint:strict:src`, a deterministic source-tree walker that excludes only generated Prisma output and runs ESLint with errors-only strict rules across every backend TypeScript file; CI now executes this gate before migrations. The command passes with zero errors, alongside application typecheck/lint, architecture validation, and the multi-target Nest build. Frontend lint remains an honest open item because the web apps currently lack ESLint configuration, and platform-admin dependencies are not installed in this workspace.
- BO-01/BO-02/BO-04/BO-06: closed a customer-web error and analytics type-safety slice. Six saved-cart route handlers now catch `unknown` errors through one safe message helper instead of reading `error.message` from `any`; browser analytics metadata and GA configuration now use `unknown` for extension fields. Customer TypeScript, OpenAPI drift checking, and the production Next build pass (70/70 generated pages). The customer workspace has no local Prettier binary, so its existing formatting conventions were preserved manually; frontend lint configuration remains a separate open item.
- BO-01/BO-02/BO-04/BO-06: closed the admin store-management error boundary. Store listing, create/update, and status-toggle handlers now catch `unknown` errors through the shared admin backend message helper instead of reading `err.message` from `any`. Admin TypeScript, OpenAPI drift checking, and the production Next build pass (99/99 generated pages); frontend lint configuration remains a separate open item.
- BO-01/BO-02/BO-04/BO-06: closed the customer saved-cart UI error boundary. Cart save/share, shared-cart import/account actions, and saved-cart load/import/delete handlers now catch `unknown` errors through the shared customer message helper instead of reading `err.message` from `any`. Customer TypeScript, OpenAPI drift checking, and the production Next build pass (70/70 generated pages); frontend lint configuration remains a separate open item.
- BO-01/BO-02/BO-04/BO-06: closed the customer account type-safety slice. Reorder results now expose explicit unavailable-item contracts, delivery-zone flattening uses typed zone/district models instead of callback `any`, and reorder errors use the shared unknown-safe message helper. Customer TypeScript, OpenAPI drift checking, and the production Next build pass (70/70 generated pages); frontend lint configuration remains a separate open item.
- BO-01/BO-02/BO-04/BO-06/BO-07: hardened the customer live-chat data boundary. Socket events now use an explicit wire contract, API history is narrowed from `unknown` before mapping, and local-storage history is filtered through a chat-message guard before entering React state. Customer TypeScript, OpenAPI drift checking, and the production Next build pass (70/70 generated pages); frontend lint configuration and broader chat E2E coverage remain open.
- BO-01/BO-02/BO-04/BO-06: closed two frontend contract boundaries. Checkout public store options now use an explicit `PublicStore` model and reject non-array payloads; admin rider vehicle selectors use a closed `VehicleType` union with runtime value validation instead of `as any`. Customer TypeScript/OpenAPI/build pass (70/70 pages), and admin TypeScript/OpenAPI/build pass (99/99 pages); frontend lint configuration remains open.
- BO-01/BO-02/BO-04/BO-06/BO-07: closed the admin live-chat contract slice. Database conversation summaries, message history, and realtime socket events now use explicit contracts instead of `any`, preserving guest/customer matching and admin message routing. Admin TypeScript, OpenAPI drift checking, and the production Next build pass (99/99 generated pages); frontend lint configuration and broader chat E2E coverage remain open.
- BO-01/BO-02/BO-04/BO-06: closed the admin rider-map external-global boundary. The dynamically loaded Leaflet `window.L`, map, layer group, marker, icon, and polyline surfaces now use a minimal local interface instead of `any`; map rendering and reverse-geocoding behavior are unchanged. Admin TypeScript, OpenAPI drift checking, and the production Next build pass (99/99 generated pages); frontend lint configuration remains open.
- BO-01/BO-02/BO-04/BO-06: closed the shared customer Leaflet boundary. MiniAddressMap and LocationPickerModal now import one minimal Leaflet contract for dynamic globals, map/marker refs, draggable events, and click coordinates instead of `any`; map and geocoding behavior are unchanged. Customer TypeScript, OpenAPI drift checking, and the production Next build pass (70/70 generated pages); frontend lint configuration remains open.
- BO-01/BO-02/BO-04/BO-06: closed the remaining admin delivery-map Leaflet boundary. Delivery-map refs, dynamic `window.L`, map layers, markers, and popup callbacks now use a shared admin Leaflet contract; the rider location modal reuses the same contract and its map-data lookup no longer uses an explicit `any`. Admin TypeScript, OpenAPI drift checking, and the production Next build pass (99/99 generated pages); frontend lint configuration remains open.
- BO-01/BO-02/BO-04/BO-06: hardened platform-admin client response boundaries. Login, organization actions, usage reconciliation, migrations, and plan editing now parse JSON as `unknown` through shared record/message/data helpers instead of reading unvalidated `.message` and `.data` fields; migration actions also stop silently reloading after failed requests. Platform-admin TypeScript, OpenAPI drift checking, and the production Next build pass (14/14 generated routes); frontend lint configuration remains open.
- BO-01/BO-02/BO-04/BO-06: centralized customer and tenant-admin BFF envelope parsing. Shared backend, cart, refresh-session, and proxy helpers now normalize unknown JSON payloads into validated envelope fields before exposing messages, correlation IDs, codes, or generic data to callers. Customer TypeScript/OpenAPI/build pass (70/70 pages), and admin TypeScript/OpenAPI/build pass (99/99 pages); frontend lint configuration and broader E2E coverage remain open.
- BO-03/BO-06: removed fake infrastructure digests from the production Compose CI gate. The production workflow now resolves Postgres, Redis, MinIO, and MinIO client tags to real SHA-256 manifest digests at job runtime, rejects unresolved references, and then runs the existing overlay exposure/security assertions. Local development Compose remains unchanged; deployment still requires operator-managed immutable image variables and a real ingress/TLS strategy.
- BO-01/BO-02/BO-04/BO-06/BO-07: closed the mobile application type-safety slice. Mobile API error parsing now narrows unknown JSON; catalog screens use typed category/product responses; Expo links use `Href`; YouTube review errors use unknown-safe catches; and live-chat socket/history payloads are structurally narrowed before matching or rendering. Mobile application `any` inventory is empty and `pnpm typecheck` passes; mobile lint and E2E coverage remain open.
- BO-02/BO-06/BO-07: established the customer-web lint gate. Added pinned Next 14 ESLint dependencies and a non-interactive `next/core-web-vitals` configuration, wired read-only lint into CI, and fixed two category-navigation hook-order violations plus coordinate/socket hook dependency warnings. Customer lint now reports zero errors; remaining warnings are image optimization recommendations. Customer TypeScript, OpenAPI drift, lint, and production build pass (70/70 pages).
- BO-02/BO-06: established the tenant-admin lint gate. Added the pinned Next 14 ESLint toolchain/configuration, wired read-only lint into CI, and fixed the store-management unescaped-quote error. Tenant Admin lint now reports zero errors; remaining warnings are hook-dependency and image optimization recommendations. Tenant Admin TypeScript, OpenAPI drift, lint, and production build pass (99/99 pages).
- BO-02/BO-06: established the platform-admin lint gate. Added the pinned Next 14 ESLint toolchain/configuration and wired read-only lint into CI. Platform Admin lint reports zero warnings or errors; TypeScript, OpenAPI drift, lint, and production build pass (14/14 routes).
- BO-02/BO-06/BO-07: established the mobile lint gate. Added the pinned Expo ESLint toolchain and explicit flat configuration, excluded generated Expo output from the lint boundary, fixed two real JSX lint errors, and wired read-only lint into CI. Mobile lint now reports zero blocking errors and mobile TypeScript passes; non-blocking source warnings and native/E2E coverage remain open.
- BO-01/BO-04/BO-06/BO-09: removed proven-unused Prisma migration compatibility artifacts from the common library: the Mongoose-named exception filter, unexported Mongoose-shaped base entity, generic CRUD service/controller, and their exports. Updated stale service comments to describe the active tenant-aware Prisma boundary. Common build, architecture validation, strict backend source lint, application typecheck, and tenant database fail-closed tests pass.
- BO-01/BO-02/BO-04/BO-06/BO-09: closed the full-backend typecheck and strict-lint regression exposed by the legacy cleanup pass. Jest mock contracts now use explicit argument tuples, tenant fan-out callbacks preserve `Promise<void>` without misleading async wrappers, and the tenant registry fixture type is declared in scope. Full backend TypeScript, complete strict source lint, production build, and five focused security/tenant/report/warranty suites pass (16 tests).
- BO-04/BO-06/BO-07/BO-09: closed the storage namespace fail-open path. `tenantObjectKey()` now rejects missing tenant identity when tenancy is enabled instead of emitting a shared `legacy/` object prefix; legacy mode remains explicit and tenant contexts continue to use immutable organization namespaces. Added regression coverage for all three modes and updated the R2 isolation contract.
- BO-04/BO-09: removed the obsolete tracked `imports_to_fix.txt` migration scratch file, which referenced deleted generic/Mongoose paths and no longer described the active Prisma architecture. The maintained Mongoose-removal architecture note remains in `docs/nest-architecture/remove-mongoose.md`.
- BO-04/BO-07/BO-09: centralized storage-key authorization with `assertTenantObjectKey()` and applied it to the storage controller plus R2 presign/delete boundaries. Direct strategy calls can no longer sign or delete another tenant's object, and tenant mode fails closed without identity. Storage namespace tests now cover four cases.
- BO-04/BO-07: normalized presigned upload folder and filename input before key construction. R2 paths now use bounded ASCII-safe segments, normalize separators, remove traversal-like dot runs, and provide a deterministic fallback for empty folders; sanitization tests cover nested, unsafe, and empty inputs.
- BO-01/BO-04/BO-07: replaced predictable `Math.random()` suffixes in platform invoice/payment references and socket fallback message IDs with cryptographically secure random bytes. Financial identifiers retain their readable prefixes and callback semantics, while generated message IDs retain their existing wire shape; platform billing, socket, application/strict typechecks, source lint, architecture validation, and production build must remain green.
- BO-04/BO-09: closed the optional tenant-provider fail-open path. Services that support the migration window now resolve through a shared `resolveTenantDatabase()` helper; a missing `TenantDbService` provider is accepted only when tenancy is explicitly disabled and raises `TENANT_DATABASE_SERVICE_REQUIRED` when `TENANCY_ENABLED=true`. Added regression coverage for both modes and migrated all 15 optional database boundaries.
- BO-03/BO-07: removed inherited fixed `container_name` values for production backend and web services, allowing Compose/orchestrator replicas instead of forcing singleton containers. Added a CI assertion preventing production application services from regaining fixed names; local development Compose remains intentionally unchanged.
- BO-03/BO-06/BO-07: separated backend liveness from readiness. Added a fail-closed `/api/v1/ready` probe that verifies PostgreSQL and Redis, changed the backend Compose healthcheck to use it, and retained `/api/v1/health` as a lightweight process-liveness endpoint. Added regression coverage for missing readiness providers; dependency outage behavior remains 503 instead of a false healthy signal.
- BO-04/BO-09: migrated the operations-health database probe to the shared tenant database resolver. In tenancy mode, a missing tenant database provider now reports unavailable evidence instead of probing the legacy Prisma client; the existing health response remains non-throwing and redacts infrastructure errors.
- BO-04/BO-09: migrated the authentication, two-factor, cart, checkout, commerce-payment, commerce-settings, store-location, product-content, and storefront-analytics database boundaries to `resolveTenantDatabase()`. These high-risk paths now fail closed when tenancy is enabled and their optional provider is absent.
- BO-01/BO-02/BO-06: aligned the cart abandoned-eligibility test contract with the active privacy predicate requiring a non-empty verified user email. The focused commerce/identity regression batch now validates the actual query shape instead of masking this existing behavior mismatch.
- BO-04/BO-09: migrated user-management, shipping, refunds, returns, service-booking, and customer-notification database boundaries to `resolveTenantDatabase()`. Identity, delivery, and financial tenant paths now fail closed when tenancy is enabled and their optional provider is absent.
- BO-04/BO-09: completed the remaining active feature-service fallback migration for product requests, purchase activity, RTO, settlement imports, staff access, transactional messaging, and warranty. The active source tree now has no direct optional `tenantDb ? getOrLegacy(...) : prisma` database boundary.
- BO-04/BO-09: removed the obsolete `prisma/mongo_db_compatible.md` migration scratch note. It described a superseded Mongoose starter structure, had no code or documentation references, and no active Mongoose dependency/import remains; the maintained removal architecture note remains in `docs/nest-architecture/remove-mongoose.md`.
- BO-02/BO-06: made the general backend lint boundary honest and reproducible. Added `tsconfig.lint.json` for source, libraries, and integration tests; ignored generated Prisma declarations; removed the nonexistent `apps` glob; and kept `lint:fix` separate from read-only `lint`. Broad lint now evaluates handwritten code instead of failing on generated declarations or project-service exclusions.
- BO-02/BO-06: separated integration/queue-smoke lint from application type-aware lint. Application source and libraries remain under the strict lint project; `test/**/*.ts` is still linted with non-type-aware rules because those tests use independent ts-jest projects and contain legacy partial infrastructure doubles. The remaining integration TypeScript/import debt is explicitly preserved for its own remediation slice.
- BO-02/BO-06: reduced the non-type-aware integration-test lint boundary to zero errors by removing one dead test helper/import, using `const` for an immutable socket client collection, and explicitly ignoring intentionally unused underscore-prefixed test-double arguments. Remaining integration lint output is warning-only and remains separate from strict application source lint.
- BO-01/BO-02/BO-06/BO-07: closed the remaining 15 backend floating-promise warnings. Cloudinary's callback bridge is explicitly marked as intentionally fire-and-forget, while Socket.IO room joins/leaves and tenant family-room joins are awaited before handlers continue. The affected attachment and socket files now pass strict ESLint with zero errors or warnings, application typecheck, and Prettier validation; the broad lint boundary remains warning-only because 1,291 formatting warnings are still tracked separately.
- BO-02/BO-06: reduced the broad backend formatting backlog in a bounded production-source slice. Prettier was applied to the highest-warning commerce, identity, platform, tenancy, messaging, configuration, and shared-library files; broad lint remains at zero errors and dropped from 1,291 to 1,033 warnings, all Prettier-only. Application typecheck, strict source lint, production build, diff validation, and 33 focused commerce/auth/platform/tenancy tests pass; the remaining formatting backlog is intentionally tracked for later reviewable slices.
- BO-02/BO-06: reduced the next production-source formatting slice across shared security utilities, Redis modules, platform services, OAuth, service booking, and tenancy. Broad backend lint remains at zero errors and dropped from 1,033 to 946 warnings, all Prettier-only. Application typecheck, strict source lint, production build, diff validation, and 24 focused security/platform/OAuth/service-booking tests pass; remaining formatting debt is still tracked for bounded slices.
- BO-02/BO-06: completed the handwritten production-source formatting boundary across `src` and `libs` (excluding generated Prisma output). Broad backend lint remains at zero errors and dropped from 946 to 788 warnings; production source now has zero formatting warnings, and all remaining warnings are confined to test files. Application typecheck, strict source lint, production build, diff validation, and 26 focused catalog/checkout/shipping/storage/warranty/platform tests pass.
- BO-02/BO-06/BO-07: completed the backend lint cleanup across handwritten source, unit specs, integration specs, and queue-smoke specs. The full configured boundary now evaluates 442 files with zero errors and zero warnings; the final 35 warnings were formatting debt in the plan-limit integration fixture. Application typecheck, strict source lint, production build, diff validation, and 39 focused tenancy/auth/cart/payment/socket/plan-limit tests pass.
- BO-02/BO-06: promoted the clean full backend lint boundary into CI. The backend workflow now runs `pnpm lint` after strict source lint, covering `src`, `libs`, and `test` files instead of enforcing only the active entrypoint and production-source checks. The local 442-file lint run passes with zero errors and zero warnings; this closes the regression gap for the completed lint remediation.
- BO-03/BO-06: aligned the backend Dockerfile healthcheck with the dependency-aware readiness contract. The image now probes `/api/v1/ready` instead of liveness `/api/v1/health`, and CI asserts that this readiness endpoint remains in the backend Dockerfile. Compose already used readiness; image and deployment health semantics are now consistent.
- BO-07: added an opt-in dependency-free HTTP capacity smoke runner at `scripts/http-capacity-smoke.mjs`, exposed as `pnpm test:http-capacity`. Operators can target a running backend with bounded request count/concurrency/timeout settings and receive structured throughput, p50/p95/p99/max latency, completion, and failure evidence; non-2xx responses and transport failures fail the command. Local success and failure-path verification passed (25 successful requests and 7 intentional failures); this is repeatable capacity evidence, not a million-concurrent-user claim.
- BO-06/BO-07: wired the HTTP capacity runner into the backend CI job against the built Nest process and disposable PostgreSQL/Redis services. CI now waits for liveness, measures 500 concurrent-safe liveness requests, and measures 100 dependency-aware readiness requests, emitting structured latency/throughput evidence and failing on errors. This establishes repeatable single-instance HTTP baseline evidence; multi-instance, WebSocket fanout, queue saturation, and database/Redis exhaustion tests remain open.
- BO-07: extended the real wire-level Socket.IO isolation integration test with a bounded tenant-fanout baseline. Fifty authenticated clients (25 per tenant) connected in 130 ms in the local run; an org-A notification reached all 25 org-A listeners in 1 ms and reached zero org-B listeners. The existing CI integration suite now carries this tenant-scoped fanout evidence; larger fanout, multi-instance adapter behavior, queue saturation, and database/Redis exhaustion tests remain open.
- BO-06/BO-07: added a real BullMQ queue-capacity smoke spec to the existing isolated Redis queue-smoke boundary. It enqueues a bounded tenant-labelled batch (configurable, default 500 jobs), drains it with controlled worker concurrency (default 10), verifies exact org-A/org-B job counts, and emits enqueue/drain throughput evidence. Static gates pass; local runtime execution is deferred because Redis on the isolated test port is not running, while CI already provisions the required disposable Redis service. Production queue saturation and multi-instance throughput remain open.
- BO-06/BO-07: added a real PostgreSQL tenant-pool capacity check to the existing performance suite. It executes concurrent tenant queries against a fresh database, samples `pg_stat_activity`, verifies the observed session peak stays within `TENANT_DB_POOL_MAX`, and emits structured pool-bound evidence. Static gates and resolver performance tests pass locally; the PostgreSQL-backed measurement is CI-gated because this shell has no `TEST_DATABASE_URL`. Database connection exhaustion, multi-instance behavior, and production capacity limits remain open.
- BO-06/BO-07: added a real Redis capacity/recovery smoke spec to the isolated queue-smoke boundary. It runs a bounded tenant-prefixed pipeline, verifies all writes and reads, measures command throughput, disconnects and reconnects the client, and confirms post-reconnect commands succeed. Static lint/typecheck pass; local runtime execution remains CI-gated because the isolated Redis port is not running here. Redis server restart/failover, memory saturation, and multi-instance recovery evidence remain open.
- BO-07: added a CI-gated two-instance Socket.IO integration test using the real Redis adapter. A tenant-scoped event emitted by instance A is verified on the same-tenant client connected to instance B and rejected by a foreign-tenant client; structured fanout latency is emitted. Static checks pass and the suite skips safely without `TEST_REDIS_PORT`; local runtime execution is deferred because Redis is not running here. Larger fanout, adapter failover, and autoscaled deployment evidence remain open.
- BO-03/BO-07: added `check:connection-budget`, a CI-enforced PostgreSQL budget calculator covering backend replicas, the legacy Prisma pool, the platform pool, the bounded tenant-client cache, tenant pool size, and reserved headroom. The default one-replica configuration passes at 90 theoretical connections against 90 usable connections; an intentional two-replica run is rejected at 180/90. The gate prevents replica/pool changes from silently exceeding the assumed database tier; the real managed-PostgreSQL limit and PgBouncer/orchestrator policy still require production configuration.
- BO-06/BO-07: extended the HTTP capacity runner to accept comma-separated `CAPACITY_BASE_URLS` and distribute requests round-robin while reporting per-target counts and failures. CI now starts two built backend processes with an explicitly reduced CI connection budget, waits for both liveness endpoints, and measures both liveness and readiness across both instances. Local temporary-server verification split 20 requests 10/10 with zero failures; this is direct multi-process evidence, not proof of load-balancer behavior, autoscaling, or million-user capacity.
- BO-06/BO-07: made scalability evidence durable in CI. Connection-budget JSON, multi-instance HTTP capacity JSON, and both backend startup logs are now uploaded as a 14-day artifact even when the capacity step fails. This preserves auditable throughput and startup evidence for tuning; it does not replace long-running load tests or production observability retention.
- BO-07: added a real BullMQ scheduler idempotence check to the Redis queue-smoke suite. Two queue clients concurrently register the same stable scheduler ID and the test asserts Redis retains exactly one scheduler, protecting recurring jobs when backend replicas start together. Static checks pass; runtime evidence remains CI-gated because the isolated Redis service is unavailable locally.
- BO-06/BO-07: added a non-secret `process.instanceId` to the operations-health payload, preferring `INSTANCE_ID`, then container hostname, then PID. This lets operators correlate queue counts, pool pressure, and readiness evidence to a specific backend replica; focused operations-health tests, application typecheck, and strict lint pass.
- BO-03/BO-07: bounded backend graceful shutdown with configurable `SHUTDOWN_TIMEOUT_MS` (default 25 seconds), duplicate-signal protection, forced exit on stuck dependency teardown, and a 30-second production Compose grace period. This prevents rolling deployments from hanging indefinitely on sockets, queues, or database cleanup. Application typecheck, strict lint, production build, and formatting pass; Docker Compose validation was unavailable locally because the installed Snap refused execution without AppArmor confinement.
- BO-01/BO-02/BO-04/BO-06: refactored the tenancy bounded context into explicit NestJS role folders. Controllers, services, guards, queues, processors, utilities, tenant context, errors, and all tenancy tests now have separate ownership folders; every feature, platform, application, and integration-test import was updated without changing routes, providers, or behavior. Tenancy tests pass with 16 suites and 111 tests; application typecheck, architecture validation, strict source lint, formatting, and diff validation pass. The original audit remains unchanged.
- BO-01/BO-02/BO-04: removed production NestJS DI escape hatches. `PlatformAdminController` now receives `TenantSchemaBootstrapper` from the container instead of constructing it per request, and `ProvisioningService` now uses the required `TENANT_DB_PROVISIONER` token without a manual `LocalPostgresProvisioner` fallback. The provisioner dependency is immutable; application typecheck, architecture validation, strict source lint, production build, focused platform/tenancy tests (32 tests), and diff validation pass. Redis dependencies were intentionally left unchanged.
- BO-01/BO-02: reduced platform-controller DI fan-in by moving platform authentication, plan/subscription catalog, migration, and support-access routes into focused controllers. All routes remain under the same `platform` prefix with the same guards and permission metadata; the main controller retains organization lifecycle, billing, dashboard, usage, and health responsibilities. Application typecheck, architecture validation, strict source lint, and focused platform tests pass. Optional tenant compatibility injections and Redis dependencies remain intentionally unchanged for a separate migration-boundary wave.
- BO-01/BO-02/BO-04/BO-06: completed the tenancy DI boundary wave. `TenancyModule` is now a global application infrastructure module, and tenant database/fanout injections are required Nest providers instead of optional runtime dependencies; missing tenancy wiring now fails during application startup rather than silently selecting a legacy path. Updated the store-locations unit test to register an explicit tenant-db double. Application typecheck, architecture validation, strict source lint, formatting, production build, and the full backend suite pass (98 suites, 447 tests). Redis dependencies were not changed; unrelated optional health, retention, and integration dependencies remain separately tracked.
- BO-01/BO-02/BO-04/BO-06: completed the entitlement DI boundary wave. Catalog and order services now require `EntitlementsService` and `UsageService`; staff access now requires the plan-gate and active-member counter tokens, preventing plan enforcement from silently disappearing when providers are misconfigured. Removed unused retention queue/processor constructor injections. Updated direct-constructor tests with explicit doubles. Focused DI tests pass (4 suites, 25 tests); application typecheck, architecture validation, strict source lint, production build, formatting, and the full backend suite pass (98 suites, 447 tests). Redis and health-probe optional dependencies remain intentionally untouched.
- BO-07/BO-09: hardened MT-11 tenant migration execution with bounded retries for known transient PostgreSQL/Prisma failures and a per-tenant bootstrap timeout. Retry count, exponential backoff, and timeout are environment-configurable with safe bounds; permanent failures still record per-tenant failure and health evidence. Added regression coverage for transient lock contention; focused migration tests pass (5/5), with application typecheck, strict lint, architecture validation, and backend build passing. Release 1 migration validation, backup/restore exercises, rollback evidence, and production operations remain open.
- BO-03/BO-07: hardened the MT-12 backup/restore runbook boundary. Tenant backups now produce custom-format dumps, verified `pg_restore` listings, SHA-256 sidecars, JSON manifests with database/timestamp/schema-version metadata, and fail when migration history cannot be verified. Restore drills require a checksum, a new `restore_drill_*` target, refuse existing databases, and verify completed Prisma migration history after restore. Added static shell-contract regression coverage and passed shell syntax, focused tests, Prettier, and strict lint; provider scheduling, credential protection, and a live restore exercise remain open.
- BO-04/BO-06: closed the platform authorization realm test boundary. Added direct coverage proving tenant-realm JWTs cannot access platform routes, valid platform tokens attach only a platform principal, login bypass is limited to the credential endpoint, and platform permissions are enforced. The guard spec passes 5/5; application typecheck, strict lint, architecture validation, and backend build remain green. Payment/callback/object/rider cross-tenant scenarios and live deployment proof remain open.
- BO-03/BO-07: closed the managed-pooling design slice without claiming deployment. ADR-0003 now defines the measured escalation trigger, PgBouncer transaction-pooling constraints, direct-session exceptions for migrations/restore/operations, bounded timeout/failure behavior, connection-budget inputs, and required two-tenant/multi-instance rollout evidence. The bounded LRU manager remains the default until the trigger is reached; provider provisioning and production capacity evidence remain open.
- BO-03/BO-06: added a deterministic Prisma migration preflight to CI. `check:migrations` validates both tenant and platform PostgreSQL migration roots, lock providers, timestamped names, duplicate names, and non-empty SQL before either migration deploy runs. The local gate validates 46 migration directories; existing disposable PostgreSQL integration and build gates remain required. Compatibility rollout testing and live tenant-fleet migration evidence remain open.
- BO-07: added bounded migration-fleet observability. Tenant migration starts, per-database success/failure, pause, and completion now increment the shared capped `TenantMetrics` counters; regression coverage asserts success and completion signals without exposing credentials or unbounded run IDs. The migration spec passes 5/5, with application typecheck, strict lint, formatting, architecture validation, and backend build gates remaining green.
- BO-04/BO-06: closed the Platform Support grant expiry/revocation test boundary. Added 8 focused cases covering minimum reason validation, TTL clamping, scoped active lookup, expiry/revocation fail-closed behavior, idempotent revoke, audit emission, and organization-filtered listings. The support-access spec and strict source lint pass; auditing every downstream tenant-data action and live support-session visibility remain open.
- BO-04/BO-06: closed the support-access usage audit boundary. `assertActive` now records `SUPPORT_ACCESS_USED` with grant, organization, and platform actor identity, and it fails closed when the append-only audit write fails. Added regression coverage; support access remains explicit and constrained, while live provider/pilot validation remains open.
- BO-01/BO-04/BO-06: hardened the support-access API boundary with dedicated DTOs. Platform support grant requests now validate bounded organization/reason/scope fields and transform/constrain TTL values to 5 minutes through 8 hours; listing accepts only an optional organization filter. Added DTO regression coverage; downstream tenant-data enforcement and live provider/pilot validation remain open.
- BO-01/BO-04/BO-06: hardened platform organization lifecycle mutations with dedicated DTOs. Organization creation now validates canonical slugs and owner email, while status transition, provisioning idempotency, and closure requests use bounded runtime contracts. Added five regression cases; service-level lifecycle and production-provider validation remain open.
- BO-01/BO-04/BO-06: hardened plan and subscription mutations with nested DTO validation. Plan keys, display names, pricing, billing interval, entitlement keys/limits, trial duration, and subscription status are now bounded before platform services execute. Added four regression cases; entitlement behavior and live billing-provider validation remain open.
- BO-01/BO-04/BO-06: hardened platform billing and migration controls with dedicated DTOs. Invoice dates, callback outcomes, provider references, and migration canary/concurrency/failure thresholds are validated before service execution. Added four regression cases; live payment-provider and fleet migration validation remain open.
- BO-01/BO-04/BO-06: closed platform financial invariant gaps. Invoice creation now rejects invalid, equal, reversed, or non-finite periods before database access; plan creation now applies the same normalized entitlement/key/amount validation as updates. Added 7 focused service tests; real billing-provider verification remains open.
- BO-04/BO-06/BO-07: fixed migration canary selection. The requested `canaryOrganizationId` is now persisted in the platform schema/migration and honored by the orchestrator before the remaining ordered fleet; a non-ready canary fails the run without touching other tenants. Added regression coverage; live multi-tenant migration drills remain open.
- BO-04/BO-06: closed the provisioning idempotency race boundary. Concurrent duplicate keys now recover the winning run, completed replays return the existing result, and cross-organization key reuse fails with `PROVISIONING_IDEMPOTENCY_KEY_CONFLICT`. Added focused regression coverage; live partial-failure recovery drills remain open.
- BO-04/BO-06/BO-07: closed the manual shipping queue tenant boundary. Operator-triggered courier polling and callback retries now require resolved tenant context in tenancy mode, use the tenant database for callback lookup, stamp `organizationId`, and namespace job IDs. Added focused queue coverage; queue dead-letter retention and live multi-tenant worker drills remain open.
- BO-01/BO-04/BO-06: made organization creation atomic across the control-plane organization and initial owner membership. Service-level name/email invariants are enforced independently of DTO validation, and unique conflicts distinguish slug reuse from owner-membership conflicts. Added lifecycle regression coverage; physical provisioning, tenant smoke tests, and partial-failure compensation remain open.
- BO-03/BO-04/BO-06: closed the tenant activation readiness gap. Provisioning now opens the registered tenant database, runs a bounded read-only connectivity probe, verifies the migration ledger and baseline tables, and records a separate `SMOKE_TEST` step before activation can proceed. Real PostgreSQL bootstrap integration coverage exercises the readiness probe; physical-hosting/provider validation and compensation workflows remain open.
- BO-04/BO-06/BO-07: removed the migration resume HTTP execution path. Paused tenant migration runs now re-enter the same deterministic BullMQ job boundary as initial runs, so operator requests return immediately and fleet work remains retryable, observable, and isolated from proxy timeouts. Added regression coverage; the physical 10-database validation exercise and recovery runbook remain open.
- BO-04/BO-06/BO-07: fixed failed-tenant migration recovery. Failed result rows are no longer treated as completed, and failed canary runs can be resumed through the same queued path; successful tenants remain skipped. Added regression coverage; operator recovery runbook and physical multi-database failure drill remain open.
- BO-03/BO-04/BO-06: hardened provisioning replay boundaries. Retries now reuse an active platform subdomain, an existing tenant database registry, and an already-active organization instead of repeating side effects. Added focused replay coverage; provider-level cleanup for a physical database created before registry registration still requires the recovery runbook.
- BO-03/BO-04/BO-06: added the provisioning partial-failure runbook. It documents same-run replay, failed-step recovery, orphan physical-resource review, activation safeguards, escalation boundaries, and evidence requirements without introducing unsafe automatic database deletion. Automated provider cleanup and managed-hosting procedures remain intentionally open.
- BO-04/BO-06: closed the suspended-tenant mutation boundary. A global tenancy guard now preserves storefront reads and authentication while rejecting non-read requests for suspended tenants with the stable commerce-mutation error; the existing service utility remains available for defense in depth. Added regression coverage for commerce writes, reads/preflights, authentication, and requests without tenant context. Historical access/downgrade policy and production deployment validation remain separate work.
- BO-04/BO-06/BO-07: closed the migration pause race. A queued migration worker now honors a durable `PAUSED` run state before touching any tenant database, preventing an operator pause from being bypassed by an already-enqueued job. Added regression coverage and marked the platform rollout-pause control complete; live multi-instance operator drills remain open.
- BO-01/BO-04/BO-06: closed the platform domain-health slice. Added credential-free `GET /platform/domain-health` diagnostics with per-domain and organization lifecycle status, stable attention reasons, focused service coverage, and a Platform Admin console page. DNS/TLS provider verification and production wildcard infrastructure remain operational work.
- BO-01/BO-02/BO-06: advanced the OpenAPI bootstrap DI remediation without changing Redis. Catalog, staff-access, order, and shipping queue boundaries now use explicit runtime Nest tokens instead of union/import-type metadata that resolved to `Object`; the exporter reaches Redis initialization, where this shell has no local Redis service. Remaining optional tenant-service boundaries and isolated Redis-backed contract execution remain tracked.
- BO-01/BO-04/BO-06: closed the manual SaaS billing control boundary. Invoice creation and hosted payment initiation now require the existing `saas_billing:write` permission plus a bounded operator reason, and append actor/reason audit evidence after the control-plane mutation. Gateway callbacks remain unauthenticated but server-validated and idempotent; live billing-provider verification remains open.
- BO-01/BO-04/BO-09: closed the platform permission catalog gap. Platform permissions now have a typed canonical registry covering organization, subscription, billing, domain, provisioning, migration, support access, tenant database, usage, audit, and health capabilities; database/domain health diagnostics require the dedicated `platform_health:read` permission. Role expansion and live authorization drills remain operational follow-up.
- BO-01/BO-04/BO-07: closed the tenant audit-context gap. The immutable tenant context now carries the trusted request correlation ID, and the shared tenant `AuditService` automatically records safe organization/database/domain/hostname context with every audit row. Structured logging remains credential-free; queue and live multi-tenant observability drills remain open.
- BO-01/BO-04: closed the local tenant-host mapping gap. `TENANT_DEV_HOST_MAP` supports explicit exact aliases such as `localhost:3000=store.ferio.local` only outside production; production ignores the setting and remains host-registry fail-closed. Resolver tests cover both modes.
- BO-01/BO-04/BO-06: closed the internal subscription audit gap. Explicit activation of the approved `internal` plan now records an actor-attributed `SUBSCRIPTION_INTERNAL_STARTED` platform audit event with organization, plan, and ACTIVE-state evidence; duplicate subscriptions and inactive internal plans remain rejected.
- BO-04/BO-06: closed the implicit platform-to-tenant access gap. `TenantMembershipGuard` now rejects platform-realm principals before tenant membership lookup, even when the operator email matches a tenant roster; platform tenant-data access must use the explicit support-access workflow.
- BO-01/BO-04: closed the reserved-slug provisioning gap. Organization creation now rejects system subdomains such as `admin` before the control-plane transaction, preventing a `PROVISIONING` organization from being created only to fail later during domain reservation.
- BO-01/BO-04/BO-06: closed the SaaS billing isolation gate. The architecture check now rejects tenant-plane imports and tenant database access from `PlatformBillingService`; existing control-plane billing tests document that invoices and payment attempts cannot enter tenant commerce ledgers.
- BO-02/BO-04/BO-06: closed the warranty media isolation gap. Warranty evidence now injects the shared `STORAGE_STRATEGY` from `StorageModule` instead of the legacy Cloudinary adapter, so tenant-mode uploads use private R2 keys derived from trusted `TenantContext`.
- BO-04/BO-06: closed the authenticated socket-ticket boundary. `SocketAuthController` now requires tenant membership before issuing a ticket, preventing a valid user session from another tenant from receiving a ticket bound to the current host.
- BO-04/BO-06/BO-09: closed the Tenant Admin controller coverage gap. The architecture gate now scans every feature `@Controller('admin/...')` class and fails if `TenantMembershipGuard` is absent; focused metadata tests retain coverage for settings, delivery-personnel, conversations, and socket-ticket method-level routes. Public, customer, rider, and authentication routes remain explicit exceptions rather than receiving a blanket guard.
- BO-03/BO-04/BO-06/BO-07: closed the tenant connection-manager observability gap. `TenantDatabaseManager` now records bounded client-eviction and capacity-exhaustion metrics alongside acquisition failures and breaker events; regression coverage proves LRU eviction and active-client capacity rejection. The manager already provides trusted registry-only resolution, encrypted credentials, single-flight caching, idle eviction, acquisition timeouts, circuit breaking, and graceful shutdown.
- BO-04/BO-06/BO-07: closed the background tenant-context retry gap. Platform-triggered retention retries now enter an immutable context built from the trusted control-plane registry before deleting tenant data; the retention regression suite verifies every tenant query observes the resolved organization. Queue envelopes, tenant fan-out processors, and authenticated socket authorization now have documented context propagation evidence.
- BO-03/BO-04/BO-06: closed the provisioning domain-activation race. Platform subdomains now start as `PENDING_ACTIVATION` and become resolvable only after migration, seed, readiness, and smoke-test steps complete; added the platform migration, explicit activation service, provisioning wiring, and regression coverage. Organization lifecycle transitions now use compare-and-set updates so concurrent operators cannot overwrite each other's state or append false lifecycle history.
- BO-01/BO-04/BO-06/BO-07: hardened the shared entitlement evaluator. Product creation now passes an increment of one instead of double-counting the current catalog, limit comparisons remain exact for bigint usage values, invalid requested counts fail before usage reads, and staff-seat enforcement delegates to the same evaluator as orders and products. Added focused regression coverage for large limits, invalid increments, delegation, and stable denial codes; Redis and the mobile project were not touched.
- BO-01/BO-04/BO-06: closed the custom-domain plan gate. `DomainsService.addCustomDomain` now evaluates `custom_domain` before creating a pending verification record and returns the shared stable denial code when the organization is not entitled; focused domain tests cover both allowed and denied registration. DNS/TLS provider automation remains intentionally blocked as an infrastructure decision.
- BO-04/BO-06: closed entitlement fail-open paths. Product creation, order placement, and staff invitations no longer convert control-plane or member-counter errors into an allowed request; required evaluator/counter failures now abort before the commerce mutation. Added evaluator propagation coverage and retained stable denial behavior for normal plan decisions.
- BO-04/BO-06/BO-07: closed the cross-tenant saved-cart token evidence gap. Added a two-tenant isolation test proving the same share token is queried only from the trusted tenant database selected by context; tenant B cannot resolve tenant A's saved cart. Redis and mobile behavior were not changed.
- BO-04/BO-06/BO-07: closed the payment callback tenant-binding evidence gap. Added a negative regression proving a callback token signed with another secret is rejected before tenant routing or payment processing; valid callbacks remain bound to the HMAC-selected organization and tenant-local database.
- BO-04/BO-06/BO-07: closed the cross-tenant wallet evidence gap. Added a two-tenant test using the same customer ID to prove wallet balances and transaction ledgers resolve from separate tenant databases. Redis and mobile behavior were not changed.
- BO-04/BO-06/BO-07: closed two Release 1 isolation evidence gaps. Rider GPS tests prove overlapping rider user IDs write only to the current tenant's personnel/location records, and storage controller tests reject another organization's object prefix before presigning. Redis and mobile behavior were not changed.
- BO-04/BO-06/BO-07: closed the post-purchase and pickup ownership-test slice. Added two-tenant coverage for returns, refunds, RTO cases, courier settlements, store pickup locations, and storefront search analytics; each service reads identical lookup identifiers only from the trusted tenant database. Broader analytics/report/audit coverage remains open.
- BO-03/BO-04/BO-06/BO-07: advanced scalability evidence without changing Redis. Added a bounded 100-tenant fan-out simulation and reconciled the checklist with existing real-PostgreSQL connection-manager/pool benchmarks, capacity-exhaustion tests, control-plane outage tests, connection-budget enforcement, and Redis collision coverage. Million-user capacity is still unclaimed; HTTP, WebSocket, queue, noisy-neighbor, and multi-instance production evidence remain open.
- BO-03/BO-04/BO-06: reconciled Release 1 migration and closure evidence. Credited the canonical tenant/platform schema split, immutable migration validation boundary, control-plane schema-version tracking, retry/resume/incompatible-tenant tests, queued fleet orchestration, closure domain revocation, and tested closure runbook. Physical fleet migration, managed backup/PITR, restore drills, and retention-policy approval remain open.
- BO-04/BO-06/BO-07: closed the customer identity and reorder ownership evidence slice. Added two-tenant tests for overlapping user/customer IDs and tenant-local reorder denial, and credited the existing global-identity/membership policy, session replay, rider personnel, tenant-global order-history, and audit-context evidence. Customer-facing SSR/BFF and full pilot-flow validation remain open.
- BO-04/BO-06/BO-07: closed the tenant-local customer metrics and reporting evidence slice. Added two-tenant tests for overlapping customer/order IDs proving customer metrics, report overview, and the bounded permission-masked orders export use only the resolved tenant database. Provider credentials, live-map, and broader production pilot validation remain open.
- BO-04/BO-06/BO-07: closed the tenant-scoped manual retry boundary. Reconciliation retries now require tenant context, carry the trusted organization, and use an organization-prefixed job ID; shipping callback and transactional-message retry paths retain the same context and audit guarantees. Dead-letter retention and live worker drills remain open.
- BO-03/BO-04/BO-06: closed the business-neutral tenant baseline seed evidence slice. Added regression coverage proving provisioning seeds only tenant-local settings and COD policy with the organization name, preserves the factory fallback safely, and inserts no fake customer/order/payment records. Notification-template defaults and managed provider configuration remain intentionally open.
- BO-01/BO-04/BO-06: closed the Platform Admin aggregate privacy evidence slice. Added dashboard coverage proving organization, subscription, database, provisioning-failure, and support-grant results are control-plane counts only and do not expose tenant identifiers or PII. Actual support-data viewing remains gated behind the explicit, audited workflow and is still open.
- BO-04/BO-06: closed the overdue subscription behavior slice. Tenant resolution regression coverage proves `PAST_DUE` stores remain browsable during the approved grace period, while Tenant Admin plan coverage proves active members can still see `PAST_DUE`, period-end recovery metadata, usage, limits, and domains. Commerce suspension restrictions and billing recovery remain separate lifecycle controls; Redis and mobile behavior were not changed.
- BO-04/BO-06/BO-07: reconciled existing Release 1 evidence for explicit support access, database-per-tenant isolation, and plan downgrade preservation. The support-access unit matrix proves reason/TTL/scope/audit/revocation and fail-closed use; the CI PostgreSQL bootstrap suite proves two independent databases, overlapping identifiers, isolated writes, and rollback; the plan-limit lifecycle suite proves historical orders survive downgrade. Managed infrastructure, backup/restore, and full pilot execution remain open.
- BO-03/BO-04/BO-06: closed the migration contention evidence slice. Tenant bootstrap bounds PostgreSQL lock and statement duration, orchestrator retries known transient lock/deadlock failures, and migration execution is queue-driven rather than startup-wide. Provider backup/restore and migration compatibility policy remain open.
- BO-04/BO-06: closed the support-access audit-link evidence slice. Every support grant use is tied to the exact organization and platform operator, emits `SUPPORT_ACCESS_USED`, and fails closed when the audit write is unavailable.
- BO-01/BO-03/BO-06: closed the bounded provisioning and platform-billing metrics slice. Provisioning lifecycle counters and billing invoice/payment outcome counters now flow through `TenantMetrics` into the existing structured snapshot without tenant PII or financial identifiers; focused tests prove the metric contracts.
- BO-01/BO-04/BO-06: closed the SaaS invoice/receipt projection slice. Platform Admin can retrieve a bounded receipt only for a paid control-plane invoice, with the successful provider reference when available; unpaid invoices, raw gateway payloads, and tenant commerce ledgers remain inaccessible through the receipt contract.
- BO-03/BO-04/BO-06/BO-07: closed the Release 1 architecture-evidence slice. Added the canonical application/plane map, tenant-boundary PR checklist, threat model, and media ownership/retention policy; reconciled the checklist with existing platform/tenant authorization evidence, opaque cross-plane IDs, and two-tenant fan-out outage tests. Production ingress, backup/restore, provider lifecycle, and frontend E2E evidence remain open.
- BO-03/BO-04/BO-07: added slow-tenant fan-out evidence. A bounded worker test proves healthy tenants progress while one tenant is delayed, without changing Redis or claiming production queue fairness; noisy-neighbor queue behavior remains open.
- BO-02/BO-04/BO-06: closed the storefront SEO and metadata tenant-boundary slice. Customer Web now checks trusted tenant status before metadata/sitemap/robots generation, derives public URLs from the forwarded host, disables indexing for inactive/unknown stores, and keeps server fetches host-forwarded/no-store for branding and catalog safety. Full SSR/BFF E2E remains open.
- BO-01/BO-03/BO-06: closed the Platform Admin system-health projection slice. Added permission-protected control-plane database, queue, tenant-registry, backup/restore-evidence, support-grant, and bounded metric health output with secret/error redaction and focused degradation tests. Provider-backed backups, alert routing, and disaster-recovery exercises remain open.
- Release 1 checklist integrity: corrected nine malformed completed markers in MT-7 so inventory, payment-recovery/reconciliation, shipment, and rider isolation work is counted by the release progress calculation. No partial or unverified controls were changed.
- MT-9 cache recovery: added a permission-protected organization domain-cache invalidation action. It derives hostnames from the control plane, delegates eviction through the existing tenancy hook, and records bounded audit evidence; callers cannot supply cache keys or connection material.
- MT-9 status reconciliation: marked the already-delivered INTERNAL entitlement and credential-free domain verification diagnostics as complete, citing the approved PO-002 policy, audited internal subscription flow, and `/platform/domain-health` evidence. Restore workflow, branding cache invalidation, and support scope enforcement remain open.
- Release 1 status reconciliation: marked five existing controls as complete from direct code/test evidence: bounded migration canary/batch rollout, separate tenant/platform health surfaces, tenant-prefixed BullMQ IDs, append-only support grant/use/revoke auditing, and prominent active-support visibility in the dashboard and console. Policy-dependent support authorization and scope enforcement remain open.
- MT-10 onboarding: extended the existing Tenant Admin Store Setup checklist with validated currency/timezone, order-prefix, subscription, and primary-domain readiness checks, and reconciled the corresponding setup-wizard, identity, plan-summary, and domain-status checklist controls. Provider/courier configuration remains open.
- MT-10 entitlement UX: added first-product guidance to the setup checklist, an explicit plan review/upgrade action, and an 80% near-limit warning before hard entitlement denial. Full navigation plan gating and historical-record downgrade UX remain open.
- MT-10 navigation entitlements: exposed typed boolean plan features through `GET /tenancy/my-plan` and applied them to Tenant Admin Sidebar presentation filtering. Backend permissions and entitlement checks remain authoritative; downgrade-history UX remains open.
- MT-13 backup freshness: platform and tenant operations-health projections now emit a bounded `backup_freshness_observed` metric labeled only by backup and restore state. This makes stale/missing backup evidence alertable through the existing structured metrics snapshot without exposing secrets or tenant identifiers; provider scheduling, restore drills, and alert routing remain open.
- MT-10/MT-11 storefront evidence: reconciled nine directly verified controls: tenant-scoped usage/limit summary, store name, Hero Showcase, support contacts, policy links, catalog routing, support information, SEO behavior, and unknown/suspended domain states. Logo/social/theme customization, tenant-aware cart/account/checkout/payment/tracking/wallet/services/chat, SSR/BFF E2E, and branding cache invalidation remain open.
- MT-03/MT-05/MT-13 security and routing evidence: reconciled four existing controls: per-tenant DB circuit-breaker/backoff, custom-domain pending-verification state, tenant-specific support/policy settings, and mandatory CI gitleaks scanning. Provider-level rotation, DNS/TLS operations, and unproven tenant commerce-flow E2E remain open.
- MT-09/MT-12 operations evidence: reconciled three bounded controls: restore-status visibility in Platform system health, normal tenant lifecycle operations through guarded APIs/console rather than SQL shell access, and rejection of implicit platform-admin access to tenant data. Provider restore execution, live drills, and support-scope enforcement remain open.
- MT-01 architecture evidence: reconciled four existing controls: platform billing is separated from tenant payment/wallet ledgers, control-plane startup owns an independent platform database boundary, Platform Admin authorization uses its own realm/permission guard, and the organization/domain/plan/subscription/registry foundation passes the active backend typecheck/build gates. Disposable control-plane migration integration remains open.
- MT-07 commerce-boundary evidence: reconciled seven implementation-level controls from the completed service sweep and isolation suites: tenant-aware catalog/settings cache boundaries, protected-controller tenant-boundary coverage, cart/session routing, checkout/payment routing, tracking analytics, wallet ledgers, and warranty/service/chat routing. Customer identity migration, provider credential/account tenancy, and full pilot/E2E exit gates remain open.
- MT-07/MT-08 commerce evidence: reconciled tenant-prefixed product media keys, tenant-scoped reconciliation scheduling/idempotency, and per-tenant COD staff-confirmation behavior from the R2/storage tests, reconciliation integration coverage, and two-tenant vertical suite. Provider credentials, storage lifecycle/retention, and full pilot/E2E exit gates remain open.
- MT-10 onboarding evidence: reconciled tenant-local COD policy configuration and notification-template configuration from the guarded Tenant Admin routes and tenant-service wiring. Payment/courier provider configuration, credential tenancy, and provider activation remain open.
- MT-08/MT-10 security evidence: reconciled the no-arbitrary-script/CSS default and secret-redaction controls from typed commerce settings, credential-free registry/health projections, redacted webhook logging, and regression tests. Branding customization, provider credential tenancy, encryption/rotation, and production secret-manager work remain open.
- BO-03/BO-06/BO-07: made released migration artifacts tamper-evident. Added SHA-256 checksums for all 48 tenant and platform migration SQL files and extended `pnpm check:migrations` to fail on modified, missing, or unlisted artifacts. This closes the immutable-artifact control; expand/contract design, backup-before-risky-migration, compatibility testing, fleet proof, and rollback/restore evidence remain open.
- BO-04/BO-06: closed tenant-local notification-template provisioning. `TenantSchemaBootstrapper.seedBaseline` now inserts the approved order/shipment templates idempotently for every new tenant, while the messaging policy remains disabled until an approved provider is configured; the baseline seed test proves no fake customers, orders, or payments are introduced.
- BO-03/BO-04/BO-06: completed the guarded platform domain lifecycle surface. Added validated, permission-protected routes for custom-domain request/challenge, organization-scoped ownership verification, primary-domain selection, and disable/revocation; these reuse the existing entitlement, cache invalidation, uniqueness, and audit guarantees. DNS/TLS automation and live provider verification remain infrastructure work.
- BO-03/BO-04/BO-06: reconciled the existing manual SaaS billing adjustment control. Platform Admin invoice creation and hosted payment initiation require `saas_billing:write` plus a bounded operator reason, persist only in the control plane, and emit actor/reason audit events; automated retry/recovery sweeps and live provider verification remain open.
- BO-02/BO-04/BO-06: reconciled the existing storefront metadata and removed-domain safety controls. Customer Web resolves tenant status before metadata/sitemap/robots generation, reads tenant-local store settings for active metadata, emits noindex/unavailable output for non-active hosts, and the resolver fails closed for disabled and closure lifecycle states; cross-host SSR/CDN E2E evidence remains open.
- BO-01/BO-04/BO-06: restored the active backend typecheck after tenant-context and principal contracts became stricter. Updated affected isolation, queue, socket, plan, report, and provisioning test fixtures with typed correlation IDs, principal email, and current Prisma mock members; the application typecheck and 62-test focused boundary suite now pass.
- BO-03/BO-04/BO-06: reconciled owner and schema-compatibility controls with the approved architecture. Organization creation atomically creates the control-plane `OWNER` membership without duplicating a tenant-local super-admin, while stamped tenant schema versions and fail-closed `TENANT_MIGRATION_REQUIRED` routing provide the application compatibility boundary; provider entitlements and live migration drills remain open.
- BO-04/BO-06/BO-07: reconciled the provider-readiness health control. `OperationsHealthService` exposes bounded payment/courier readiness and launch blockers, and its degradation tests confirm provider/database/queue failures are redacted rather than leaking credentials or raw errors; actual provider configuration and tenant credential tenancy remain open.
- BO-03/BO-04/BO-06: reconciled the duplicate tenant-export checklist entry. The implemented Release 1 export contract is the bounded, permission-masked, tenant-routed orders export with two-tenant evidence; unsupported customer/media export surfaces remain outside that contract.
- BO-04/BO-06/BO-07: closed the order-reference tenant-isolation evidence gap. Added `order-reference.tenant-isolation.spec.ts`, which submits the same human-readable reference and verified phone inside two trusted tenant contexts and proves each lookup uses only its selected tenant database. Redis and the mobile project were not touched.
- BO-01/BO-04/BO-06/BO-09: reconciled five existing Release 1 controls with direct repository evidence: accepted ADR/product-owner architecture decisions, complete Prisma model/data classification, no active Mongoose runtime boundary, clean full-history gitleaks scan, and tenant-local reorder ownership coverage. Plan version history, provider configuration, support scope enforcement, backups, load evidence, and pilot gates remain open.
- BO-03/BO-04/BO-06/BO-07: closed four documented Release 1 controls without changing Redis: inventoried the active BullMQ queue registry/processors, defined expand/migrate/contract sequencing, prohibited one-step destructive migration changes, and added the migration rollback/forward-fix runbook with backup gates, pause/retry, isolated restore, and fail-closed schema compatibility evidence. Physical fleet drills and provider-backed backups remain open.
- BO-04/BO-06/BO-07: closed the tenant GPS retention control. `RetentionSweepService` applies the configurable `RETENTION_GPS_DAYS` policy (90 days by default) to tenant-local `DeliveryLocationHistory`, and the retention suite verifies pruning, disabled rules, batching, and trusted tenant context. Provider-backed storage lifecycle remains separate.
- BO-01/BO-04/BO-06: closed the support-access authorization/scope gap. `SupportAccessService` now validates bounded resource/action scopes, rejects unknown or wildcard permissions, requires the exact organization and platform-user grant, and fails closed when a required scope is absent; 11 focused tests pass. Provider-backed support operations remain separately open.
- BO-04/BO-06/BO-07: closed the tenant moderation/admin-queue boundary. Product-content review and banner moderation routes require tenant membership plus permission guards, and `ProductContentService` resolves moderation records through the tenant database; quick replies/folders remain open because no backend persistence contract exists.
- BO-03/BO-04/BO-06: closed the tenant provisioning default-seed gap. `TenantSchemaBootstrapper.seedBaseline` now idempotently creates the tenant-local courier catalog with every provider disabled and no credentials, while `CommerceSettings.prepaidEnabled` remains false until configuration; the bootstrap regression proves the provider defaults are separate from fake commerce data. Runtime payment/courier credentials and provider account mapping remain open because adapters still require the tenant-secure integration configuration work.
- BO-04/BO-06/BO-07: closed the tenant payment-configuration slice. Added tenant-local `CommercePaymentProviderConfig` records with AES-256-GCM credentials, a guarded admin update route with strict provider allowlists and masked readiness, tenant-context gateway injection, prepaid enablement checks against tenant configuration, and encryption/runtime regression coverage. Courier credential tenancy, provider vault rotation, and courier account mapping remain open.
- BO-04/BO-06/BO-07: closed the tenant courier-configuration slice. Added tenant-local `CourierProviderConfig` records with AES-256-GCM credentials, a permission-protected provider configuration route with strict per-courier allowlists and masked readiness, and async-local credential injection for all six courier adapters. Recommendation, polling eligibility, shipment creation, activation, callback verification, and retry paths now resolve credentials within the trusted tenant context; no tenant request falls back to another tenant's courier credentials. External vault rotation, managed provider onboarding, and production courier account verification remain open.
- BO-04/BO-06/BO-07: closed the tenant live-map event slice without changing Redis. Tenant-bound admins join `delivery-live-map`, and successful rider GPS updates emit `rider-location-updated` through the ambient organization room only; focused delivery and socket tests prove the event cannot select a foreign tenant from payload data. Production fanout/load evidence remains open.
- BO-03/BO-04/BO-06: reconciled the tenant database boundary markers with current evidence. The active commerce service sweep uses the shared `TenantDbService`/`resolveTenantDatabase` boundary, production configuration rejects legacy tenancy mode, and the disposable PostgreSQL suite proves tenant-local transaction rollback without affecting another tenant. Direct singleton dependency removal, nested-service static enforcement, managed pooling design, and production load evidence remain open.
- BO-03/BO-04/BO-06: reconciled the cross-plane transaction and provisioning-saga controls. Platform code keeps control-plane transactions separate from tenant writes, ADR-0001 plus the partial-provisioning runbook define recorded saga/replay/repair boundaries, and the disposable PostgreSQL suite supplies rollback isolation evidence. Provider-specific orphan cleanup and managed-hosting recovery remain open.
- BO-04/BO-06/BO-07: advanced tenant-scoped transactional messaging. Added tenant-local encrypted provider configuration records, guarded provider management endpoints, credential-key-only responses, rotation/audit evidence, and adapter inputs resolved from the active tenant database. The registry remains fail-closed because no concrete SMS/WhatsApp/email adapter is currently implemented; external provider delivery and live E2E remain open.
- BO-04/BO-06: reconciled the quick replies/folders control as not applicable to Release 1. No configurable or persisted quick-reply/folder capability exists in the PRD/schema, so there is no tenant data path to isolate; the control must be reopened if that feature is later introduced.
- BO-01/BO-04/BO-06/BO-07: reconciled six Release 1 exit-summary controls with direct evidence: provisioning idempotency, migration orchestration, subscription/entitlement enforcement, platform-vs-tenant billing separation, unknown/suspended-domain behavior, and tenant-prefixed Redis/BullMQ/WebSocket/object isolation. Live pilot, production DNS/TLS, backup/restore, and security acceptance gates remain open.
- BO-04/BO-06: closed the applicable session-adjunct boundary. Host-only cart/customer/rider cookies, forwarded storefront hosts, tenant-local commerce records, and tenant-bound access checks prevent session state from crossing storefronts; the opaque refresh-token blacklist remains intentionally platform-global under the approved shared-identity decision.
- BO-02/BO-04/BO-06: advanced upload-boundary validation without claiming malware scanning. Admin presigned uploads now use an allowlisted MIME set and a signed size limit, while buffered warranty evidence additionally verifies JPEG/PNG/WebP file signatures before storage. Post-upload inspection for direct PUTs and malware scanning remain open, so the Release 1 control stays explicitly partial.
- BO-03/BO-04/BO-06: hardened scheduled retention shutdown behavior. The fleet retention sweep now selects only READY databases whose organization is ACTIVE, matching the existing tenant fan-out lifecycle boundary; regression coverage proves CLOSED organizations are skipped. Explicit queued-job revocation and provider-backed retention operations remain open.
- BO-03/BO-04/BO-06: closed the callback lifecycle boundary. Tenant callback routing now loads the organization lifecycle status with the trusted database registry and rejects non-ACTIVE organizations before warming a tenant client; focused tests cover CLOSED rejection and ACTIVE success. Explicit queued-job revocation remains open.
- BO-01/BO-04/BO-06: reconciled the conditional Google/Meta integration control as not applicable to Release 1. No integration is enabled or persisted in the current release, so there is no tenant data path to isolate; the checklist must be reopened before any Google/Meta connector is introduced.
- BO-03/BO-04/BO-06: closed the targeted tenant-job lifecycle gap. `TenantFanoutService.forOrganization` now requires both a READY registry and ACTIVE organization before warming a tenant client, preventing queued retry work from mutating closed tenants; focused coverage proves inactive jobs fail before client acquisition. Explicit queue-job revocation remains open.
- BO-03/BO-04/BO-06: closed the direct retention-retry lifecycle gap. `RetentionSweepService.sweepTenant` now applies the same ACTIVE-organization guard as fleet retention and targeted fan-out, preventing a queued retention retry from pruning a closed tenant; regression coverage proves client acquisition is skipped. Explicit queue-job revocation remains open.
- BO-01/BO-03/BO-04/BO-06: closed the silent closure-finalization gap. The closure API and service now require an explicit retention confirmation before retiring tenant registries, including after the recoverable window; regression coverage proves missing confirmation cannot finalize or mutate databases.
- BO-02/BO-04/BO-06: hardened the Customer Web tenant edge. Incoming `x-forwarded-host` is ignored by default and is accepted only when `CUSTOMER_WEB_TRUSTED_PROXY=true`; forwarded-host chains are rejected instead of truncated. Server fetches, BFF routes, tenant status, middleware origin checks, sitemap, and robots now share the same host-selection policy. Customer Web typecheck and lint passed, backend architecture checks passed, and live two-host SSR/BFF E2E remains intentionally open.
- BO-03/BO-06: reconciled two previously stale owner-decision blockers. PO-007/PO-008 select Cloudflare DNS with wildcard subdomains and automated wildcard TLS as the production direction, and PO-013 approves the 90-day recoverable closure window with legal/financial retention controls. The checklist now separates those approved decisions from still-open DNS/TLS execution, export, backup, and physical deletion work.
- BO-03/BO-06: separated wildcard certificate strategy from certificate operations. The approved Cloudflare-managed automated wildcard TLS direction is now recorded as complete; issuance, renewal monitoring, DNS records, and readiness verification remain operational exit work.
- BO-02/BO-04/BO-06: verified the Customer Web host boundary under the supported runtime. The Node 20 production build compiled, typechecked, generated all 71 pages, and finalized optimization; a disposable BFF smoke test sent distinct `Host` values with forged `x-forwarded-host` values and confirmed each response used the direct host. The full registered-tenant SSR/BFF E2E gate remains open because this smoke test does not replace real domain registration, tenant fixtures, or browser-level coverage.
- BO-03/BO-04/BO-06: added the Release 1 tenant portability package. `scripts/export-tenant.sh` emits a checksum-verified `ferio-tenant-export-v1` package with tenant business, audit, and financial records plus schema metadata; the runbook requires trusted registry resolution, private handling, encryption, and audit evidence. Media blobs, provider-backed deletion, credential revocation, and mandatory closure attestation remain open by design.
- BO-01/BO-03/BO-06: reconciled the Product-Owner/Architecture Decision Checklist with the existing decision log and implementation evidence. Resolved directions for trial/grace behavior, SaaS billing provider, hostname, managed PostgreSQL, credential encryption, RPO/RTO, retention, identity scope, object-key strategy, and onboarding are now marked complete; blocked plan treatment and pending legal review remain unchecked.
- BO-04/BO-06/BO-07: closed tenant integration credential revocation. Payment and courier admin routes now delete encrypted tenant provider configurations in a transaction, disable courier activation, audit only non-secret metadata, and return safely on repeated revocation; focused payment/shipping tests pass. External provider-side credential invalidation remains an infrastructure/provider follow-up.
- BO-04/BO-06: refreshed the committed OpenAPI artifact after adding the payment/courier revoke routes; the repository drift check now passes and no Redis source/configuration was changed.
- BO-02/BO-04/BO-06: added fail-closed R2 tenant media lifecycle primitives. Tenant-prefix listing and batched deletion derive `tenants/{organizationId}/` from ambient context, reject missing context, and never accept a client-selected organization or legacy prefix; focused storage tests pass. Provider bucket lifecycle policy and automatic closure orchestration remain open.
- BO-01: completed the remaining legacy feature-folder naming migration by renaming `features/socket.gateway` to `features/socket-gateway` and updating all source/integration imports. No runtime behavior or Redis key contract changed; validation is limited to the checked-in application and socket integration boundaries.
- BO-02/BO-04/BO-06: added a controlled R2 media portability sidecar. The helper accepts only a trusted organization identifier, derives `tenants/{organizationId}/`, records the provider key listing, downloads only that prefix, and writes checksums; closure automation and provider lifecycle policy remain open.
- BO-03/BO-04/BO-06: closed the export-before-closure contract gap. Finalization now requires explicit retention and export attestations, records a secret-free `TENANT_EXPORT_ATTESTED` audit event, and the Platform Admin caller sends both confirmations; the actual operator export remains a prerequisite rather than an unverified automatic claim.
- BO-04/BO-06/BO-07: closed the tenant-job stop boundary without touching Redis. Scheduled fan-out and retention sweeps enumerate only READY databases for ACTIVE organizations, while targeted queued work for closed/unavailable organizations fails before tenant-client acquisition; this is safer than mutating shared queue state during closure.
- BO-04/BO-06/BO-07: closed the MT-7 high-risk isolation evidence gate. Added overlapping-ID two-tenant coverage for reconciliation findings and payment state selection, then indexed the existing checkout/COD, wallet, post-purchase, identity, WebSocket, and worker suites in `skill-related-discussion/high-risk-two-tenant-test-matrix.md`. The focused financial suites pass 23/23; Redis and the mobile project were not touched. Production-scale capacity and full pilot execution remain open.
- BO-04/BO-06/BO-07: closed the tenant-scoped distributed-lock gap without touching Redis. Direct-conversation PostgreSQL advisory lock keys now include the trusted organization before the sorted participant IDs, and a two-tenant regression proves identical participants cannot share a lock key; legacy mode keeps the prior key shape.
- BO-02/BO-04/BO-06/BO-07: reconciled the host-boundary test gates with committed evidence. Forged/malformed host, unknown-domain, cross-tenant token/session, forwarded-host chain, trusted-proxy, Customer Web build, and disposable two-host BFF smoke checks are complete; live registered-tenant browser/pilot execution remains correctly tracked under MT-14 rather than blocking the application-level test controls.
