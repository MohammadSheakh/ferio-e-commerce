- 2026-09-10: Started backend remediation from the September audit. Production tenant-edge trust was made explicit in `docker-compose.production.yml`; a fail-closed `check:tenant-edge-policy` validator and architecture regression check now require tenancy, public domain, explicit customer-web forwarded-host policy, and non-zero trusted proxy CIDRs. This validates deployment configuration only; live DNS, tunnel peers, managed infrastructure, legacy fallback removal, and external provider evidence remain open.
- 2026-09-10: Completed the tenant-edge remediation batch. Added `CUSTOMER_WEB_TRUSTED_PROXY` to `.env.example` and recorded the command evidence in `project-progress/2026-09-10-backend-edge-policy-evidence.md`. The implementation still does not claim live DNS/tunnel, managed recovery, provider delivery, legacy fallback removal, or security acceptance.
- 2026-09-10: Regression-validated the backend remediation: strict source lint, application lint/typecheck, architecture checks, focused storage/tenancy tests (`39/39`), and the full backend suite (`142` suites, `645` tests) passed. Expected Redis-unavailable warnings remained fail-closed test behavior; Redis implementation/configuration was not changed.
- 2026-09-10: Strengthened the legacy compatibility boundary. `resolveTenantDatabase` and `TenantDbService.getOrLegacy` now require a non-empty fallback reason, all tenant feature call sites provide an explicit service reason, and `architecture:check` rejects undocumented fallback calls. This does not remove legacy mode; it makes remaining compatibility use reviewable and prevents new silent fallbacks.
- 2026-09-10: Recorded explicit-fallback evidence in `project-progress/2026-09-10-explicit-legacy-fallback-evidence.md`. Full backend regression remains green at `142` suites/`645` tests; legacy fallback removal, live infrastructure, providers, pilots, scale evidence, and formal security acceptance remain open rather than being marked complete.
- 2026-09-10: Closed a concrete tenant-context bypass in `ShippingWebhookQueue.health()`: recoverable callback counts now use the ambient tenant database instead of direct `PrismaService` access. Added tenant-mode regression coverage and architecture enforcement against direct Prisma queries in feature queues/processors; Redis and mobile code remain untouched.
- 2026-09-10: Recorded queue/worker boundary evidence in `project-progress/2026-09-10-tenant-context-boundary-evidence.md`. The full backend suite passed `142` suites/`645` tests; queue fairness, dead-letter retention, Redis behavior, provider delivery, and multi-instance capacity remain external/operational gates.
- 2026-09-10: Added the machine-readable `scripts/tenant-context-boundaries.json` inventory and `check:tenant-context-boundaries` validator for HTTP, workers, sockets, and tenant fan-out. `architecture:check` also requires a valid inventory with at least ten entry points; this is structural regression protection, not a replacement for runtime isolation or production load evidence.
- 2026-09-11: Validated the tenant-context inventory across HTTP, eight worker/queue boundaries, socket authentication/rooms, and tenant fan-out. The new validator, architecture gate, strict source lint, application lint/typecheck, focused tenancy suite (`15/15`), and full backend suite (`142` suites, `645` tests) passed. This closes repository-level context-inventory drift protection, but not live ingress, queue fairness, provider, scale, or production acceptance evidence.
- 2026-09-11: Hardened CI by running tenant-context, architecture, PostgreSQL connection-budget, and production tenant-edge contract gates before backend build/tests, plus ESLint in all three web jobs. Local gates and workflow YAML validation passed; browser E2E, live two-host SSR/BFF, managed recovery, scale, provider, pilot, and formal security acceptance remain open.
- 2026-09-11: API integration shot 01 found and fixed a customer storefront tenancy bug: `GET /tenancy/status` is globally enveloped by NestJS, but `lib/tenancy.ts` read `code` at the top level. The client now unwraps `{ success, data, message }`, retains an older unwrapped-staging compatibility path, and fails closed for malformed payloads. Customer web typecheck and lint passed; full API/screen/live-host verification remains a separate multi-shot task.
- 2026-09-11: API integration shot 02 found and fixed a platform-admin BFF method gap: the catch-all route accepted only GET/POST/PATCH while the platform OpenAPI surface also exposes PUT/DELETE. The BFF now forwards all five methods through the server-side platform session. Platform-admin typecheck and lint passed; browser authorization/UX and production provider behavior remain open.
- 2026-09-11: API integration shot 03 found and fixed a tenant-admin browser-call gap: the store setup checklist requested `/api/admin/delivery-zones`, but the implemented BFF route is `/api/delivery-zones`. The call now reaches the authenticated tenant-scoped BFF. Tenant-admin typecheck passed and lint passed with existing non-blocking warnings; 104 route files were statically checked, but browser E2E, live two-host SSR/BFF, provider behavior, and formal release gates remain open.
- 2026-09-11: The same tenant-admin API shot found stale generated OpenAPI types: `lib/api-schema.ts` lacked five newer endpoint groups and two provider-config DELETE operations present in `openapi.json`. Regenerated the schema and restored the `api:check` gate; this closes generated-contract drift, not browser E2E or production evidence.
- 2026-09-11: API integration shot 04 audited the customer storefront: 102 browser/shared-library source files mapped to 51 Next.js BFF routes with no unresolved path or method mismatch. Refreshed stale customer OpenAPI types for storage finalization; customer `api:check` and typecheck passed, while lint retained existing non-blocking image warnings. Live browser, host-isolation, SSR/BFF, socket, provider, and production gates remain open.
- 2026-09-11: API integration shot 05 reviewed customer login, registration, OAuth, email verification, refresh/logout cookie rotation, socket-ticket, and chat-message BFF flows against the NestJS controllers. No verified route or method mismatch was found, so no code change was required. Source-level evidence was recorded; browser cookies, live tenant forwarding, WebSocket room isolation, and production identity-provider behavior remain runtime gates.
- 2026-09-11: API integration shot 06 audited customer cart, checkout, payment, wallet, and tracking flows. The implementation matched the backend routes, but API docs incorrectly listed `POST /cart/validate` as GET and `/payments/initiate` as a query-based GET. Corrected both docs to the actual POST contracts and recorded the remaining duplicate-submit, provider-callback, race, live-host, and production-credential gates.
- 2026-09-11: API integration shot 07 found and fixed a shared customer-session transport defect: `Headers` instances were spread as plain objects, so wallet top-up `Idempotency-Key` headers could be dropped before reaching the backend. Header normalization now preserves idempotency/correlation headers while enforcing server-owned auth and tenant context. Account profile/address docs were corrected from PATCH to PUT; runtime replay, upload, provider, and live-isolation evidence remain open.
- 2026-09-11: API integration shot 08 found and fixed a rider portal authentication gap: order-status BFF requests read Authorization from the browser, but the rider JWT is intentionally httpOnly-cookie-only. Status transitions now use `riderTokenFromCookie()` like the other rider actions; rider action docs and evidence were expanded. Live authorization, GPS, transition-race, COD confirmation, and cross-tenant runtime evidence remain open.
- 2026-09-11: API integration shot 09 audited tenant-admin shipping and delivery-personnel operations. Corrected stale shipping docs for order-scoped shipment creation, shipment-scoped polling, callback retry, and provider config PUT/DELETE contracts. Admin rider CRUD, approval, map, location-history, and order-detail assignment flows now match their BFF/controller routes. The assignment UI filters approved tenant riders and submits the validated `{ orderId, deliveryPersonnelId }` DTO. Current-assignment display, concurrent assignment handling, live authorization, and browser E2E evidence remain open.
- 2026-09-11: API integration shot 11 audited tenant-admin store locations, delivery zones, and store pickup. Store CRUD, delivery-zone CRUD, and admin pickup status/OTP calls match their BFF/controller routes. Corrected the admin docs to remove the nonexistent admin pickup-scheduling route and documented the real customer `PATCH /orders/:id/store-pickup/schedule` contract. No customer-web scheduling caller was found, so pickup scheduling remains an explicit partial integration item for the next shot.
- 2026-09-11: API integration shot 12 integrated customer pickup scheduling. The account-order projection now includes delivery method and pickup state, a session-authenticated customer BFF PATCH route forwards `/orders/:id/store-pickup/schedule`, and account order history renders the date/time, slot, and notes form only for store-pickup orders. Customer typecheck/lint and NestJS application typecheck/lint passed; broad backend tsc remains blocked by pre-existing unrelated test typing errors. Live ownership, conflict, OTP, and host-isolation evidence remain open.
- 2026-09-11: API integration shot 13 audited customer warranty, purchase-history, and returns surfaces. Corrected stale warranty docs to claims/mine, order-items, evidence/upload, and claims contracts; warranty BFF and screen already match these routes. Corrected purchase-activity docs to the supported surface/page/limit query (no productId filter). Current NestJS exposes returns for tenant-admin only, so no customer returns caller was marked missing; live upload/content, warranty ownership, activity settings, and runtime isolation evidence remain open.
- 2026-09-11: API integration shot 09 audited tenant-admin shipping and delivery-personnel operations. Corrected stale shipping docs for order-scoped shipment creation, shipment-scoped polling, callback retry, and provider config PUT/DELETE contracts. Admin rider CRUD, approval, map, and location-history flows match their BFF/controller routes. Found an unintegrated `assign-order` BFF route with no browser caller; assignment UI, DTO validation, concurrent assignment handling, and runtime authorization evidence remain open rather than being marked complete.
- 2026-09-11: API integration shot 14 audited customer services, product requests, product-content loading, and authenticated YouTube review submission, plus the related tenant-admin operations. Source routes and methods matched the active callers. Corrected stale documentation for optional product-request authentication, review payload fields, the unfiltered admin review queue, banner PATCH/DELETE paths, and the guarded `/product-requests...` admin queue. Evidence is recorded in `project-progress/2026-09-11-customer-value-added-api-integration-shot-14.md`; browser E2E, tenant-host isolation, moderation authorization, and production provider/operational evidence remain open.
- 2026-09-11: API integration shot 15 audited tenant-admin transactional messaging, commerce settings, hero showcase, catalog/content callers, and customer notifications. Source callers and BFF routes matched the active NestJS controllers. Corrected stale docs for messaging query fields, settings POST/DELETE contracts, public `/store/config`, the `heroShowcase` enum, and notification mark-one-read/delete operations. Evidence is recorded in `project-progress/2026-09-11-admin-messaging-settings-api-integration-shot-15.md`; live provider delivery, queue capacity, browser E2E, tenant-host isolation, and production acceptance remain open.
- 2026-09-11: API integration shot 16 audited tenant-admin catalog/inventory and customer storefront discovery. Source callers matched the active BFF/controller methods and catalog DTOs. Corrected stale docs for separate brand CRUD methods, inventory pagination/search, and storefront product filters by removing unsupported `brand` query documentation. Evidence is recorded in `project-progress/2026-09-11-catalog-discovery-api-integration-shot-16.md`; browser E2E, live cache/host isolation, large-catalog performance, and production acceptance remain open.
- 2026-09-11: API integration shot 17 audited tenant-admin orders, checkout/payment operations, and fulfillment callers. Source callers matched the active BFF/controller methods. Corrected stale docs for order filter names, fulfillment/exception payloads and method, read-only payment-attempt detail, provider PUT/DELETE configuration, recovery sweep, and customer payment retry proof fields. Evidence is recorded in `project-progress/2026-09-11-orders-payments-fulfillment-api-integration-shot-17.md`; browser E2E, idempotency/race, provider callback, delivery, tenant isolation, and production acceptance remain open.
- 2026-09-11: API integration shot 18 audited tenant-admin returns, refunds, RTO, settlements, and reconciliation callers. Source callers matched the active BFF/controller methods. Corrected stale docs for item-level RTO inspection costs, return/refund DTOs, settlement idempotency headers, settlement paths, reconciliation filters, and finding action notes/enums. Evidence is recorded in `project-progress/2026-09-11-returns-reconciliation-api-integration-shot-18.md`; browser E2E, provider behavior, queue fairness, idempotency/race, tenant isolation, and production acceptance remain open.
- 2026-09-11: API integration shot 19 audited tenant-admin customers, staff, reports, analytics, audit, and operations-health callers plus related platform plan documentation. Source callers matched the active BFF/controller methods. Corrected stale docs for customer filters and nonexistent order sub-route, staff invitation/access paths, report date query names, audit filters, and platform plan PATCH; added a source-verified analytics/audit/operations contract. Evidence is recorded in `project-progress/2026-09-11-admin-platform-api-integration-shot-19.md`; browser E2E, live host isolation, analytics delivery, provider behavior, platform authorization, and production acceptance remain open.
- 2026-09-11: API integration shot 20 audited the platform-admin console and found one real validation mismatch: closure UI accepted three-character reasons while the backend requires ten. Fixed the UI minimum and corrected stale platform docs for provisioning idempotency, closure attestations, support TTL/scope, migration fields, plan payloads, trial input, and billing readiness path. Evidence is recorded in `project-progress/2026-09-11-platform-admin-api-integration-shot-20.md`; browser E2E, live authorization, managed infrastructure, provider delivery, provisioning/closure execution, and production acceptance remain open.
- 2026-09-11: API integration shot 21 completed the static route-coverage pass across customer web (52 handlers), tenant admin (104), and platform admin (3), excluding mobile. No new source-level path or method mismatch was verified after the focused customer, tenant-admin, and platform shots. Updated the API README/status ledger with the analytics/audit/operations contract and explicit runtime-proof limits. Evidence is recorded in `project-progress/2026-09-11-api-route-coverage-shot-21.md`; browser cookies, SSR/BFF host forwarding, cross-tenant isolation, WebSocket isolation, provider callbacks, idempotency races, queue fairness, and production authorization remain open.
- 2026-09-11: API integration shot 22 attempted the guarded local integration suite against the configured disposable `ferio_test_runner` database. The sandbox initially denied localhost networking; the escalated rerun reached the target but PostgreSQL was unavailable on `127.0.0.1:5433` (`ECONNREFUSED`) before migrations/tests ran. Evidence is recorded in `project-progress/2026-09-11-live-api-smoke-shot-22.md`; no live API, cross-tenant, SSR/BFF, WebSocket, callback, or idempotency evidence is claimed until the Docker PostgreSQL service is started.
- 2026-09-11: API integration shot 23 probed the configured local HTTP ports and the documented public tunnel without authenticated or mutating requests. The backend and web ports did not accept connections, and `ferio.sheakh.qzz.io` did not resolve from this environment. Evidence is recorded in `project-progress/2026-09-11-live-http-smoke-shot-23.md`; source-level API checks remain separate from live runtime evidence, and the next prerequisite is to start the local app stack and restore staging DNS/tunnel reachability.
- 2026-09-11: API integration shot 24 audited the customer checkout, order-placement, wallet, prepaid-initiation, and payment-retry contracts. Source callers and NestJS controllers/DTOs aligned. Corrected checkout documentation that incorrectly put `cartToken` in JSON; the actual BFF forwards the httpOnly cart cookie as `X-Cart-Token`. Added pickup/consent fields and wallet idempotency/header details. Evidence is recorded in `project-progress/2026-09-11-checkout-api-integration-shot-24.md`; live idempotency, atomic wallet, payment callback, pickup ownership, and tenant-host isolation remain runtime gates.
- 2026-09-11: API integration shot 25 audited customer cart mutations, validation, saved-cart sharing/import, saved-cart management, and reorder. Fixed a BFF ownership/error-boundary bug where authenticated save-cart failures fell through to an anonymous save; authenticated saved-cart errors now preserve upstream status/code/correlation data. Corrected the stale cart cookie documentation. Customer API check, typecheck, and lint passed with existing image warnings. Evidence is recorded in `project-progress/2026-09-11-cart-api-integration-shot-25.md`; live tenant-cookie separation, authorization, share-token abuse, reorder ownership, race, and checkout idempotency evidence remain open.
- 2026-09-11: API integration shot 26 audited customer catalog listing/detail, product-content, reviews, and purchase-activity surfaces. Found that the backend catalog pagination contract was not consumed by the storefront; added `page` forwarding and filter-preserving previous/next controls. Corrected discovery docs to avoid claiming the unused brand directory is integrated. Evidence is recorded in `project-progress/2026-09-11-discovery-product-api-integration-shot-26.md`; large-catalog performance, host-aware SSR/cache isolation, review auth/moderation, consent, and live tenant evidence remain open.
- 2026-09-11: API integration shot 27 audited customer login, registration, email verification/resend, Google OAuth, refresh rotation, logout, and cart-merge session flows. Source contracts aligned; hardened login/register/verify/resend/OAuth UI failure handling so network and malformed responses cannot leave controls stuck. Evidence is recorded in `project-progress/2026-09-11-auth-api-integration-shot-27.md`; cookie flags/rotation, OTP delivery/expiry, provider configuration, CSRF/origin, cart merge ownership, and live tenant isolation remain runtime gates.
- 2026-09-11: API integration shot 28 audited customer account profile/address, commerce order projection, reorder, store-pickup scheduling, notifications, and wallet/top-up flows. Corrected account docs to describe the shipped `/account/commerce` order projection instead of unsupported generic customer order endpoints. Hardened notification mutations, wallet loading/top-up submission, and account-order loading so failed or malformed responses cannot produce false optimistic state or stuck controls. Customer API check, typecheck, and lint passed with existing image warnings. Evidence is recorded in `project-progress/2026-09-11-account-api-integration-shot-28.md`; live auth, cookie/tenant isolation, notification authorization, wallet idempotency replay, provider, and operational evidence remain open.
- 2026-09-11: API integration shot 29 audited customer warranty, service booking, product request/feedback, and live chat flows. Confirmed warranty and chat routes match the tenant-bound backend contracts. Hardened service booking against duplicate submissions, malformed/network responses, and DTO length violations; constrained product-request and feedback payloads to the backend's 500-character limit. Updated value-added API documentation and recorded evidence in `project-progress/2026-09-11-value-added-api-integration-shot-29.md`; live provider/storage, booking race, multipart validation, WebSocket room isolation, and cross-tenant runtime evidence remain open.
- 2026-09-11: API integration shot 30 performed a shared customer/tenant-admin/platform-admin BFF contract sweep. Fixed the customer delivery-application BFF to preserve the backend envelope and status through the shared proxy helper instead of nesting the envelope in `data`. All three web `api:check` and TypeScript gates passed; existing lint warnings are unchanged. Evidence is recorded in `project-progress/2026-09-11-cross-app-bff-contract-shot-30.md`; live forwarding, cookies, tenant isolation, WebSocket, provider, idempotency, and operational evidence remain open.
- 2026-09-11: API integration shot 31 audited tenant-admin chat/support. Found that conversation list and message-history BFFs bypassed the shared admin refresh client and could fail after access-token expiry even with a valid refresh cookie. Migrated both to `adminApi`, preserving centralized refresh, tenant-host forwarding, correlation IDs, envelope parsing, and errors. Corrected the chat docs to the actual list/history and WebSocket contracts. Tenant-admin API check, typecheck, and lint passed with existing warnings. Evidence is recorded in `project-progress/2026-09-11-tenant-admin-chat-api-integration-shot-31.md`; live refresh, permission denial, WebSocket reconnect/room isolation, persistence, and production evidence remain open.
- 2026-09-11: API integration shot 32 audited tenant-admin reports, CSV exports, storefront analytics, operations health, and purchase activity. Date/filter contracts, export fields and row cap, response envelopes, and shared admin transport match the active callers and NestJS contracts; no source mismatch required a code change. Evidence is recorded in `project-progress/2026-09-11-reports-operations-api-integration-shot-32.md`; live authorization/masking, export limits, event freshness, health accuracy, tenant isolation, and production monitoring remain open.
- 2026-09-11: API integration shot 33 audited platform-admin organization lifecycle, migrations, support revoke, plans, billing navigation, and the platform catch-all BFF. Endpoint/payload contracts matched; hardened organization, migration, and support mutation failure states so network/malformed responses cannot silently reload or leave controls stuck. Platform-admin API check, typecheck, and lint passed with no warnings. Evidence is recorded in `project-progress/2026-09-11-platform-lifecycle-api-integration-shot-33.md`; live operator auth/refresh, idempotency, provisioning/closure, migration canary, support scope, managed infrastructure, and production acceptance remain open.
- 2026-09-11: API integration shot 34 audited platform-admin plans, subscriptions, invoices, payment attempts, and the platform BFF. Plan routes/payloads matched platform DTOs; hardened plan mutation failure states and client numeric bounds. Removed silent billing/subscription outage catches so the platform error boundary surfaces control-plane failures. Platform API check, typecheck, and lint passed cleanly. Evidence is recorded in `project-progress/2026-09-11-platform-plans-billing-api-integration-shot-34.md`; live plan authorization, refresh rotation, provider callbacks, invoice/payment data, and production billing readiness remain open.
- 2026-09-11: API integration shot 35 audited Platform Admin login, platform JWT session cookies, sign-out, protected server-side calls, and platform BFF authentication. Backend route and `{ accessToken, roles }` contracts matched; hardened login network/malformed-response handling and documented the intentional eight-hour platform token/local-cookie logout model. Platform API check, typecheck, and lint passed cleanly. Evidence is recorded in `project-progress/2026-09-11-platform-auth-api-integration-shot-35.md`; browser cookie flags, throttling, token expiry, role/permission denials, and live sign-out behavior remain runtime/security gates.
- 2026-09-11: API integration shot 36 audited platform dashboard, database/domain health, organizations, provisioning timelines, migrations, plans, and support-access read screens. Backend response shapes matched the callers; removed silent server-page catches from organizations, migrations, plans, and support access so expired/denied sessions cannot render empty operational screens with misleading controls. Platform API check, typecheck, and lint passed cleanly. Evidence is recorded in `project-progress/2026-09-11-platform-health-api-integration-shot-36.md`; live permissions, token expiry, health accuracy, migration results, and alerting remain open runtime gates.
- 2026-09-11: API integration shot 37 audited tenant-admin staff access, commerce settings, two-factor security, delivery personnel/zones, and shipping callers. Contracts matched the admin BFF and backend DTOs; hardened all security-page status/setup/confirm/disable requests so network or malformed responses are visible and never leave controls stuck. Tenant-admin API check and typecheck passed; lint passed with existing unrelated warnings. Evidence is recorded in `project-progress/2026-09-11-tenant-admin-security-api-integration-shot-37.md`; live tenant forwarding, permissions, 2FA lifecycle, staff revocation, provider callbacks, queue health, and delivery authorization remain open.
- 2026-09-11: API integration shot 38 audited tenant-admin delivery personnel, delivery zones, shipping providers, shipments, courier retries, polling, and queue-health callers. Contracts matched; reordered static `PATCH /delivery-personnel/admin/assign-order` before parameterized rider PATCH to prevent route capture, and documented that provider credential PUT/DELETE remains an operator-controlled secret path rather than a browser form. Nest typecheck and tenant-admin API/typecheck/lint passed; backend lint had existing warnings only. Evidence is recorded in `project-progress/2026-09-11-tenant-admin-courier-api-integration-shot-38.md`; live authorization, callbacks, queue fairness, provider credentials, and browser E2E remain open.
- 2026-09-11: API integration shot 39 audited tenant-admin rider assignment, delivery-personnel authorization, staff-access mutations, and their BFF routes. Confirmed guard and tenant-context coverage, preserved the static assignment-route ordering fix, and ran focused backend regression coverage: 3 suites and 22 tests passed. Evidence is recorded in `project-progress/2026-09-11-tenant-admin-authorization-api-integration-shot-39.md`; live cross-tenant denial, assignment races, staff revocation, browser forwarding, and production callback evidence remain open.
- 2026-09-11: API integration shot 40 attempted live two-tenant runtime proof. The configured `TEST_DATABASE_URL` exists, but Docker was unavailable and the escalated local integration bootstrap failed before migrations/tests with `ECONNREFUSED 127.0.0.1:5433`; API/web ports 6733/3000/3001 were also not listening. No runtime completion is claimed. Evidence is recorded in `project-progress/2026-09-11-live-two-tenant-api-readiness-shot-40.md`; start disposable PostgreSQL and the application stack before rerunning.
- 2026-09-11: API integration shot 41 audited customer rider login, profile, orders, status, online/offline, GPS, and application flows. Found and fixed the PRD FR-RIDER-005 mismatch: persisted tenant-local `DeliveryPersonnel.isOnline`, updated the service and portal hydration, added an expand migration/checksum, and covered online/offline writes with focused tests. Migration checks, Nest typecheck, customer API/typecheck/lint passed; existing warnings remain. Evidence is recorded in `project-progress/2026-09-11-rider-online-state-api-integration-shot-41.md`; live rider auth, host forwarding, assignment, status races, GPS, and browser evidence remain open.
